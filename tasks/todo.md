# Hound Shield — Task Queue

## Active

- [ ] 2026-08-14 **⛔ PRODUCTION CANNOT DEPLOY — one dashboard setting, 30 seconds, founder-only.** `#288` merged and deleted the repo-root `vercel.json` so middleware executes, but the Vercel **Root Directory** was never set to `compliance-firewall-agent`. Every production build since then fails at the first step: *"No Next.js version detected. Make sure your package.json has `next` in either dependencies or devDependencies. Also check your Root Directory setting matches the directory of your package.json file."* → **Vercel → project `compliance-firewall-agent` → Settings → Build & Deployment → Root Directory → `compliance-firewall-agent` → Save, then redeploy.** Nothing in code can fix this; `rootDirectory` is a project setting, not a `vercel.json` key, and the Vercel API exposes it only at project CREATION. **The live site is UP and healthy** (`/api/health` 200) — Vercel keeps serving the last good deployment — but it is the **pre-#292 build**, so `/terms` still has no Governing Law section and still serves the stray `", ."` artifact in §12, verified against the live HTML at 21:57 UTC. Everything merged since #288 is on `main` and NOT live.
- [x] 2026-08-14 (ponytail + houndshield): **legal surface made to hold up at LAUNCH — PR #292 MERGED (`de8a834`).** CAN-SPAM was the one hard statutory exposure: `day3`/`day7`/`day14` had **no unsubscribe link, no postal address, nowhere to record an opt-out** (15 U.S.C. §7704(a)(3),(a)(5), assessed **per message**). `lib/legal/marketing-email.ts` owns the contract and **fails CLOSED** — no `MARKETING_POSTAL_ADDRESS` means the drip stays dark rather than sending something unlawful; `/api/email/unsubscribe` answers GET + RFC 8058 one-click POST, unauthenticated by necessity (§7704(a)(3) forbids extra obligations on the recipient), token-authorised in constant time, idempotent, and **fails LOUD (503)** rather than confirming a write that did not happen; migration **034** records the opt-out, filtered at send time so the 10-business-day clock in §7704(a)(4) never starts. Transactional mail deliberately untouched (§7702(17)) — `upgrade` classified on the evidence of its own content ("receipt inside", "Payment confirmed"), not its filename. **`/terms`** gained Delaware governing law and venue where there were **none**, and stopped serving *"HoundShield is operated by HoundShield is operated by … regardless of entity status., ."* on the section naming the counterparty — found by self-review, not by any test, because every guard passed and the page returned 200. **`/privacy`** stopped publishing a retention schedule **no code implements** (`retentionDays` is read by nothing; there is no purge job) and gained no-sale/no-sharing, COPPA, incident notification and state rights. **Legal index** reordered for a US-only vendor. **`/api/health`** now reports the drip and probes `profiles.marketing_opt_out_at`. **The race resolved itself in the safest possible way:** `#288` threw the cron switch *before* this landed, but because production deploys are broken, the live deployment carries **neither** the cron **nor** the gate — so the fix and the switch will now go live in the *same* deployment, whenever the Root Directory is set. Gates: **tsc 0 both packages · 2821 tests / 201 files · lint 0 errors · build exit 0 · 16 mutations probed, 16 red.** → **FOUNDER, IN THIS ORDER: (1) set the Vercel Root Directory (above), (2) apply migration 034, (3) THEN set `MARKETING_POSTAL_ADDRESS`.** Step 3 before step 2 opens the CAN-SPAM gate onto a column that does not exist and every drip run throws while looking configured — `/api/health` names both until they are done.

- [ ] 2026-08-14 (ponytail + houndshield): **made the legal surface hold up at LAUNCH, not just serve 200 — PR #292.** Founder ask was different from the previous one: the pages exist, now make their CONTENT satisfy the law. Scope decided mid-task: **US only, no EU/UK**, and **fix the marketing emails properly** rather than delete them. **The one hard statutory exposure was armed but not yet firing.** Three onboarding emails — `day3`, `day7`, `day14` — had **no unsubscribe link, no postal address, and nowhere to record an opt-out**; 15 U.S.C. §7704(a)(3) and (a)(5) require both, assessed **per message**. There is no violation yet only because the drip has never sent: its cron was never registered (the repo-root `vercel.json` has no `crons` key, and Vercel reads only that file), so it goes live the moment **#288** merges — which is exactly why this had to land FIRST. **Fails CLOSED, which is the load-bearing part:** `canSendMarketing()` returns false with no `MARKETING_POSTAL_ADDRESS`, and both the run-level and per-recipient paths consult it, so the drip stays DARK rather than sending something unlawful. A compliance feature whose safety depends on someone remembering to finish configuring it is the same defect class as the three controls that were failing open in `/api/health` — so `/api/health` now reports the drip disabled and NAMES the missing variable, because a silently dark drip looks identical to a working one from outside. `lib/legal/marketing-email.ts` owns the whole contract (address · HMAC token derived from the service-role key with a domain separator, HKDF-style, so a leaked token never narrows the search space for the credential · footer · RFC 8058 `List-Unsubscribe` headers, required by Gmail/Yahoo from bulk senders since Feb 2024), and the footer is appended by the **send path**, not pasted into each template — seven copies of a legal notice is how six go stale, the 29-base-URL lesson again. `/api/email/unsubscribe` answers GET and POST, unauthenticated by necessity (§7704(a)(3) forbids extra obligations on the recipient), token-authorised in constant time, idempotent, and **fails LOUD with 503** rather than rendering "you are unsubscribed" over a write that did not happen. Migration **034** adds `profiles.marketing_opt_out_at`, filtered at send time so the 10-business-day clock in §7704(a)(4) never starts. **Transactional mail deliberately untouched** — `report-order`, `canceled`, `partner-welcome` and `upgrade` are relationship messages under §7702(17), `upgrade` on the evidence of its own content ("receipt inside", "Payment confirmed") rather than its filename; adding "unsubscribe" to a payment receipt invites someone to opt out of mail we are obliged to send. **Then four content defects found by READING the files.** `/terms` had **no governing law, venue or dispute terms at all** (grep for governing law / jurisdiction / arbitration / venue → 0) — a contract sold to DoD subcontractors that leaves venue to whichever court a plaintiff picks; now Delaware law and venue, US-only scope stated, small-claims and injunctive carve-outs, severability. **`/terms` §12 was rendering garbage in production:** it wrapped `controllerDisclosure()` in its own lead-in plus an empty `<strong>` and a trailing comma, and that function already returns a COMPLETE sentence, so the live page served *"HoundShield is operated by HoundShield is operated by an independent sole proprietor. … regardless of entity status., ."* — a duplicated clause and a dangling ", ." on the section that names the counterparty. **Every guard passed:** no bracketed placeholder, the entity function itself honest, the page returning 200. Found by self-review, not by a test; now guarded generically (no legal page may leave an empty inline element, none may re-prefix the disclosure). **`/privacy` published a retention schedule nothing implements** — 7/90/365/2555 days sourced from `entitlements.retentionDays`, a field **read by no code**, with no purge job and no cron but the drip. Corrected in the honest direction rather than by building a purge, because this is an audit-evidence product and shredding a customer's C3PAO evidence on a timer is a different promise from the one they bought; the field keeps a `ponytail:` marker recording DECLARED, NOT ENFORCED. Also added the parts a US buyer actually reads and were entirely absent: explicit no-sale/no-sharing (CCPA/CPRA), COPPA, security-incident notification, and state rights (VA/CO/CT/UT/TX) extended to everyone rather than gated on proving residence. **The legal index led every document with GDPR** and marked `/terms` statutory on an EU e-Commerce Directive hook — not false, but it buried the instruments that apply and implied a posture the company is not operating; US instruments now lead, EU ones marked "where it applies", `/terms` reclassified contractual, and a doc comment records the trigger (Art. 27 requires an EU representative BEFORE the first EU customer). **And `LAUNCH_BLOCKERS` was crying wolf with a test pinning the falsehood:** `hipaa-posture` and `webhook-allowlist` were both already closed (`/hipaa`; PR #286), but a test **asserted the stale one stayed blocking**, so marking it honestly done turned the build red — a guard that pins today's answer converts progress into a failure. Now asserts the PROPERTY: nothing whose summary says DONE may claim to be blocking, and a closed item must record the evidence that closed it. **Ten mutations, ten reds — and two probes were wrong before they were right**, both my error rather than a weak guard: one injected a duplicate `blocking` key that the existing `blocking: false` on the next line immediately overrode, the other asserted an unknown health VALUE should fail when an unknown value is correctly judged (it lands in `degraded`) — what that guard protects is a key being EXCLUDED from judgement. The double-prefix check also went red on the very comment explaining the bug it guards, the **sixth** guard in this repo to read its own explanatory prose; it strips comments first, with a paired test proving the stripping did not neuter it. **Honest gap:** the unsubscribe SUCCESS path was never exercised against a real database — there is none in this environment — so it is covered by a route test against a mocked service client asserting the column written, the row it is scoped to, and all four refusal paths. Gates: **tsc 0 both packages · 2795 passed / 201 files** (from 2748/199) **· lint 0 errors** (33 pre-existing warnings, none in touched files; touched files re-checked at `--max-warnings 0` to match the hook) **· build EXIT 0 · Vercel preview READY.** → **FOUNDER-OWNED, no code closes these: incorporation · `MARKETING_POSTAL_ADDRESS` in Vercel** (a PO box or registered agent satisfies §7704(a)(5); until then the drip stays dark BY DESIGN and `/api/health` says so by name) **· confirm Delaware · apply migration 034** alongside the pending 029/030/033. **#288 must merge AFTER this** — merging it registers the cron, which is what starts the drip sending. **Also confirmed:** the Supabase bot reported "no changes detected in `supabase` directory" on a PR that adds migration 034 — it watches the repo ROOT `supabase/`, the same deploy-topology mismatch as the Vercel Root Directory and the missing `crons` key. One mismatch has now silently disabled three separate subsystems.

- [ ] 2026-08-14 (ponytail + houndshield): **single-sourced the site base URL — 29 copies, all pointing at a host that redirects.** Found by probing production after #287/#289 went live. Every canonical tag, Open Graph URL, `sitemap.xml` entry, `robots.txt` and onboarding-email link was built from `process.env.NEXT_PUBLIC_APP_URL ?? "https://houndshield.com"`, declared separately in **29 files** — and `NEXT_PUBLIC_APP_URL` is **unset in production** (`/api/health` says `reset_app_url: "default"`), while Vercel 308s apex → www. So every one of them named an address that refuses to serve it: a `<link rel="canonical">` whose target redirects is a page contradicting itself, on a product whose acquisition channel is organic and AI-answer traffic. **The Stripe checkout success/cancel URL was in that set** — the buyer's return path from payment, taking an extra hop. `lib/gateway/base-url.ts` already existed because the IDENTICAL failure hit the gateway host (eight copies across two dead subdomains); nobody generalised it to the marketing surface. Now one `lib/site-url.ts`, defaulting to **www** — the host Vercel serves, CLAUDE.md records as canonical, and `base-url.ts` already prints to every customer. A codemod on `??` missed a `||`, and the new guard caught that straggler. **The guard was wrong first:** it matched any read of the env var and flagged six CORRECT files (two fall back to the request origin so a confirm link matches the user's host, two to localhost for dev, one must see the raw value to decide CORS demo mode, one exists to diagnose it) — narrowed to target the apex fallback itself, because a guard that would force correct code to change is measuring the wrong thing. Gates: **tsc 0 · 2748 passed / 199 files · lint 0 errors · build EXIT 0.**

- [ ] 2026-08-14 (ponytail + houndshield): **#287 MERGED and PRODUCTION IS LIVE — then fixed the rulebook that steers every future session.** Verified the merge the way #255 taught: `git diff origin/main <branch>` empty **unscoped**, not a MERGED badge. Squashed as `1589169`; Vercel production deployment `dpl_AFqLStCS…` reached **READY** with aliases `houndshield.com` + `www.houndshield.com` and `aliasError: null`. **Then the follow-up: `.claude/rules/*` are instructions an agent EXECUTES, and three of four were wrong.** `frontend.md` ordered "Homepage bg: `bg-[#07070b]` — never `bg-white`" and "Dark mode always: `<html className='dark scroll-smooth'>`" — `app/layout.tsx` has had **no `dark` class** for months and the landing page is light, so the next session to obey it would have repainted the live marketing site. `DESIGN.md` had the identical drift and was rewritten 2026-08-07; its sibling was missed. `database.md` said "migrations 001–004 applied" (really 001–027 + 028/031/032). `stack.md` said "Next.js 15" (really Next 16.2.12), "001-030, all applied to prod" (029/030/033 unapplied — and that shape is worse than a stale number, because it tells a reader a TABLE EXISTS when it does not), and a `settings.json` model pin removed on 2026-08-07. **The drift guard already existed and already covered `stack.md`** — but its checks matched only pattern counts and the phrase "through N", so both defects sailed past a guard written for exactly this class. Widened scope to `database.md` + `frontend.md` and added two checks: "all applied to prod" is always a violation, and the theme rule is asserted **against `app/layout.tsx` rather than against a copy of the rule**, so it flips by itself the day the app genuinely goes dark. **Three drafts of the range check were deleted rather than shipped** — it kept flagging "Applied to production: 001–027, plus 028, 031, 032", which is correct as written, and the existing "through N" check already covered the honest half; two overlapping checks where one false-positives is worse than one that discriminates, and pressuring docs toward a rounder number would have made them lie. Both new checks probe-tested (restore either old claim → red). Gates: **tsc 0 · 2742 passed / 198 files · lint 0 errors (33 warnings, pre-existing) · structure + leak guards PASS.** **AND — reading the live endpoint after deploy caught a bug 2,738 green tests could not:** `/api/health` reported `rate_limit_store: degraded_local` in production while shared rate limiting was working fine. The probe selected `key` from `rate_limit_buckets`, whose primary key is **`bucket_key`** — PostgREST errored, the catch swallowed it, and the endpoint cried wolf. For a module whose entire job is reporting degradation, the false-positive direction is the dangerous one: a lying alarm teaches the operator to ignore the page, which is worse than the hardcoded "operational" it replaced. Fixed, and both probed identifiers are now pinned against the migration DDL by a guard that fails if a column is renamed — probe-tested by reintroducing the exact bug.

- [ ] 2026-08-14 (ponytail + houndshield): **closed every remaining CODE-owned finding in the Phase 2 security audit — and found that one of its recommendations was unimplementable.** `main` was clean with no open PRs, so the queue was the leftover of `docs/SECURITY-PHASE-2-AUDIT.md`: items 8, 9, 11, 12 of its remediation table plus five smaller findings. **The find that matters: the audit's own "safe, do-it-now" fix for #2 — shorten the 400-day session `maxAge` via `cookieOptions` — is a no-op.** `@supabase/ssr` 0.12.4 spreads caller options and then overwrites the result with its own default on BOTH cookie-set paths (`cookies.js:233` and `:464`). Measured, not read: driving a real session write against a local stub GoTrue with `maxAge: 2592000` (30 days) emits a cookie carrying `maxAge: 34560000` (400 days). `secure` in the same object IS honoured — so half of #2 shipped and half was **withdrawn with the evidence written into the audit**, because a diff that reads like a session-lifetime fix and changes nothing is worse than no diff. Session lifetime is a Supabase dashboard setting; the test now pins the library behaviour so the day an upgrade honours the option, it goes red. **`/api/health` was publishing three constants** — `classifier`, `quarantine`, `audit_chain`, each hardcoded `"operational"` — alongside a literal `status: "healthy"`, while three controls fail OPEN and reported nowhere (shared rate-limit store, lockout store, CAPTCHA — `verifyCaptcha()` returns TRUE with no key, so "unset" means "answers yes", not "off"). Constants deleted per the lessons.md rule; `status` is now derived from four real probes and a `degraded` array names what is failing. **That exposed a live defect on the PUBLIC `/status` page:** it decided "operational" with its own local `new Set(["operational","connected","healthy"])`, which the vocabulary had long outgrown — so it put a warning triangle next to a row reading "houndshield.com" and told every visitor "Some services need attention" permanently, whatever the truth was. One producer now names the degraded keys; readers are told, not guessing. **`'unsafe-eval'` removed from `script-src`** (both layers, kept byte-identical) and **verified in a real browser, with a control**: the on-disk Chromium driven over CDP across 6 routes → 0 app violations, and each page made to request a script from a forbidden origin to prove the detector was not simply deaf (6/6 refused, quoting the policy). Two earlier control attempts using `eval()` reported ALLOWED on every route — **DevTools evaluation contexts are CSP-exempt and script elements created from one inherit the exemption**, so an eval-based control proves nothing; logged to lessons. **`/api/scan` had no rate ceiling at all** — unauthenticated, runs the full regex engine plus an outbound model call, and its only limit was middleware's 60/min, which does not execute in production. Now 15/min in shared Postgres, deliberately independent of whether the middleware deployment issue is ever fixed. Also: `Origin` assertion at the `requireUser()` chokepoint (same-origin established by comparing `Origin` to `Host`, so previews and the apex/www pair need no config; absent `Origin` allowed, present-and-wrong 403); raw Resend error no longer returned to the client; demo-mode CORS reflection now also requires non-production so a missing env var cannot open it; `task` bounded on `/api/agents/list`; migration **033** adds a **restrictive** deny-all on the four Better Auth tables (restrictive so a future permissive policy cannot silently re-open them — 024's RLS-with-no-policy protection is a property of the ABSENCE of code). **Two guards of my own were wrong before they were right:** the CSP drift check first matched comment prose containing "script-src" and asserted against English (fixed by anchoring to the string literal), and its JSDoc contained `*/` inside a regex and broke the file's parse. Every new guard probe-tested. Gates: **tsc 0 both packages · 2697 tests / 196 files green** (was 2648/192) · **92 proxy tests over 4 runs** · lint **0 errors** (33 warnings, DOWN from 39, none in touched files) · build EXIT 0 · coverage gates pass both (app 34.87%, proxy 70.54%) · leak guard clean · `npm audit --omit=dev --audit-level=high` 0 both. → **NEXT: PR A merges and production redeploys. PR B (delete the repo-root `vercel.json`) is a DRAFT and must NOT be merged until the founder sets Vercel Root Directory → `compliance-firewall-agent`** — that file is currently the only thing telling Vercel where the app lives, so merging it alone zero-config-builds the repo root and takes the site down. Unblocking it also registers the email drip cron, **which has never fired once** (Vercel reads only the root-directory `vercel.json`, and the root file has no `crons` key) — that additionally needs `CRON_SECRET`. Note: production could NOT be probed this session; the agent proxy denies CONNECT to houndshield.com. **Founder added mid-session: publish the refund policy.** "30-day money-back guarantee" was live on FOUR surfaces — `/pricing`, `/terms` §4, the FAQPage JSON-LD, and the order-confirmation email a paying buyer receives — with **no `/refund` page and no statement anywhere of how to claim one, what the window runs from, or how long it takes**. That is the document a procurement reviewer asks for before a $499 PO clears, and Stripe requires merchants to publish one. **The bigger defect was what `/terms` DID say:** §4 described refunds only for "paid subscriptions billed in advance on a monthly or annual basis" that you may "cancel at any time" — HoundShield sells no subscription, so the refund terms for the ONLY purchasable product were unstated while terms for products that do not exist were published. The pricing FAQ was wrong the same way and worse, because it ships inside JSON-LD that answer engines quote verbatim: "Every paid HoundShield plan can be billed annually at a **20%** discount… Contact sales for multi-tenant agency pricing" — no plans exist to bill annually, and 20% contradicts the 17% in CLAUDE.md. Same class as the #275 schema.org six-tier bug. **The window was deliberately NOT narrowed:** `lib/legal/refund-policy.ts` codifies the promise already made in public rather than reducing it, because a policy stingier than the guarantee already advertised would retroactively shrink a live commitment — so 30 days, full amount, explicitly still refundable AFTER the PDF is delivered, and the window runs from payment not delivery (the assessment runs 14 days before the PDF exists; measuring from delivery would quietly halve it). `EXCLUSIONS` is a deliberately EMPTY exported list rather than silence, so adding one later is a visible reviewed act; the guard rejects any exclusion describing ordinary use. Single-sourced like `entity.ts`/`subprocessors.ts` — three prose copies is exactly how `[COMPANY LEGAL NAME]` survived on three pages at once — with prices from `RISK_REPORT` so the policy moves if the price does. Linked from both footer variants and the sitemap, because a legal page nobody can navigate to is the same as no legal page. 16 new guards, probe-tested both ways. Final gates: **tsc 0 · 2713 passed / 197 files · lint 0 errors · build EXIT 0 · `/refund` verified serving 200 with all nine sections and the $499/$299 sourced from `RISK_REPORT`.** → **ALSO NOTED, not fixed:** the Supabase GitHub integration watches the repo-root `supabase/` directory, which holds only `001_initial.sql`; the real migrations live in `compliance-firewall-agent/supabase/migrations/`, so the bot has reported "no changes detected" for every migration since — including 028/031/032. It has never validated a real migration. **Founder then asked for "the legal pages required by EU as well as US law".** Audited the surface first: `/privacy`, `/terms`, `/dpa`, `/subprocessors`, `/acceptable-use` existed; `/cookies`, `/accessibility` and any legal index did not. **The cookie gap was the sharp one.** `components/CookieConsent.tsx` is already a CORRECT banner — analytics off until opt-in, "Accept essential" offered beside "Accept all", PostHog genuinely gated at `PostHogProvider.tsx` — but it linked to `/privacy`, whose entire cookie section was two sentences naming nothing. ePrivacy Art. 5(3) consent must be INFORMED, so the weakness was in the consent obtained, not merely in the disclosure. New `/cookies` lists three items and only three, each with an in-repo evidence path (`subprocessors.ts` discipline): the Supabase auth cookie (name verified empirically from a real session write), the versioned localStorage consent record, and PostHog's cookie. **Two things deliberately NOT listed because this domain does not set them** — Stripe's (`__stripe_mid`/`__stripe_sid`; checkout is Stripe-HOSTED, no Stripe.js loaded here) and Turnstile's (unconfigured; `/api/health` says so). A cookie policy naming cookies the code does not set is a published inaccuracy about data handling, on a site sold to buyers who check exactly that. **`/accessibility` claims PARTIAL conformance and says why** — EAA 2019/882 · EN 301 549 · ADA/508 framing, backed by the hard Lighthouse gate whose minimum is READ from `.lighthouserc.json` rather than retyped (0.90, measured 0.94–0.95) — then states what automated tooling does NOT prove: no manual audit, no screen-reader testing, no VPAT. Claiming full WCAG 2.1 AA to a Section 508 buyer would be the #275 fabricated-testimonial error made to a federal customer; a guard enforces the page never says "fully conformant". **`/legal`** indexes all nine documents with the instrument each answers to and carries the EU e-Commerce Directive Art. 5 provider identification via `controllerDisclosure()` — the honest not-yet-incorporated statement, never an invented entity. `index-registry.ts` models `basis: statutory | contractual` because an AUP is a contract term, not a statutory obligation, and the guard demands a checkable citation only from the statutory ones — inventing a statute for the AUP was the dishonest way to make the check pass. Gates: **tsc 0 · 2738 passed / 198 files · lint 0 errors · build EXIT 0 · all nine legal routes verified serving 200** with `/legal` linking to every one and the banner linking to `/cookies`. → **STILL FOUNDER-OWNED: incorporation.** GDPR Art. 13(1)(a), the e-Commerce Directive and CCPA each require a named, contactable operator; `/legal` states the interim position honestly, which is the most code can do. Founder-side setting.

- [ ] 2026-08-08 (ponytail + houndshield): **Phase 2 — made the dead guards real, and caught a 70% overstatement on the homepage. PR #277 (draft), branch restarted from main after #275 merged.** Phase 1 fixed what was visibly wrong; this fixed the checks that were supposed to catch it. **The find that matters: `lib/detection/engines.ts` computed `PATTERN_COUNT` as BUILTIN + CMMC + HIPAA, but `BUILTIN_PATTERNS` already contains all 17 CMMC and all 20 HIPAA patterns — so 53 real patterns were published as 90** on the homepage stat row, the detection-engines card, and `/demo`. Two tests asserted the inflated number and stayed green for months because they asserted the *arithmetic*, not the count. That is the NEVER-DO the brain lists first — the pattern list is MIT and in the repo, so any buyer can count it. **Four gates that could not fail:** (1) `core.hooksPath` unset → `.githooks/pre-commit` inert; now installed via a `prepare` script (the setting lives in untracked `.git/config`). Wired the FAST secrets guard, not `.claude/hooks/pre-commit.sh` — tsc+lint+full-suite per commit is minutes and gets `--no-verify`'d; CI owns slow gates. (2) Coverage: the only threshold (70%) sat in an orphaned `jest.config.js`, and the app had **no coverage provider installed at all**. Deleted jest + 3 dead devDeps, added `@vitest/coverage-v8`, thresholds set at the MEASURED floor — **app 30.32%, proxy 65.26%** (proxy read a flattering 81% until the config counted files no test imports). CLAUDE.md still claims an 80% gate; reality drifted to a third of that with nothing going red. (3) **`npm audit` was wrapped in `if !` so it could never fail — and I cited its green check in the #275 description as proof the baseline was clean. It was not: 7 high-severity advisories in prod deps.** Cleared (lockfile-only, no direct version changes, 0 vulns both packages) and the wrapper removed. (4) Lighthouse could not fail three ways over (`warn` + `|| true` + `continue-on-error`); measured real scores (a11y 0.94–0.95, SEO 1.00, BP 1.00, perf 0.67–0.84) and made a11y/SEO/BP hard gates with headroom, perf stays warn because a randomly-failing check is one people learn to ignore. **Gateway tests found two real bugs in `zero-trust.ts`, both fixed:** a window crossing midnight (`22:00-02:00`) could never match (`>= 1320 && <= 120` is unsatisfiable) so night-shift orgs were permanently locked out; and a malformed rule value threw a TypeError *out of the access decision* on user-editable data. **Registry drift guard, not a merge** — the two registries don't even share a category vocabulary (proxy: CUI/PHI/CREDENTIAL; classifier: HIPAA_PHI/FINANCIAL/STRATEGIC; only IP+PII common), so consolidation is a `ponytail:` marker and the guard pins the 18 shared CUI patterns. **Docs guard** scoped to 5 load-bearing files — `.claude/rules/api.md` said "minimum 16 patterns — never reduce" against 33 shipped, i.e. permission to delete seventeen live rules. Also removed the subscription grid still live in the open-source `proxy/README.md` ($199/$499/$999). Every new guard was **probe-tested** (remove the scan overlap → 3 fail; revert the midnight fix → 1 fail; rename a CUI pattern → 2 fail; stage a fake `sk-` key → commit blocked), then reverted and re-verified. Gates: tsc clean both · lint 0 errors (39 pre-existing, none in touched files) · **2303 tests / 174 files** (was 2220/170) · 61 proxy tests over 4 runs · build passes · bench p99 **0.806 ms** · audit clean. → **NEXT: founder reviews PR #277.** Still open: `lib/gateway` ~4,850 lines behind ~750 test lines (providers, stream-proxy, ws-handler uncovered); registries still separate; **app coverage is 30%, not the 80% CLAUDE.md claims — decide whether 80% is a real target or should be restated.**

- [ ] 2026-08-08 (ponytail + houndshield): **pre-Monday launch review — four credibility/deploy blockers fixed, PR #275 (draft).** Deep dive found the product in far better shape than the docs claim: the $499 report is wired end to end (Stripe → `report_orders` → 668-line PDF generator with SPRS deltas + Merkle root, the best-tested area in the repo), `/security` and `/trust` exist, 30 migrations applied, proxy ships 33 patterns with a Dockerfile and publish workflow. **The gap was never features.** (1) **`/about` published three fabricated testimonials** attributed to named people at named companies ("Sarah Mitchell, VP of Cybersecurity, Ridgeline Defense Systems" + 2), a "Trusted by Defense Contractors" heading, and a "1,000+ Users" timeline row — FTC 16 CFR 255 exposure and the exact thing a security-officer buyer runs down before a call. Replaced with checkable proof (counts computed from the shipped engine list, hash-chained log, Mode B boundary, CI-enforced latency); the sourced 76,598 DoD market figure was KEPT — honesty means removing false claims, not true ones. (2) **Schema.org still advertised six tiers incl. $199 Pro** that `/pricing` hasn't sold since it became single-offer — answer engines quoting prices with no checkout, and a literal second pricing grid in machine-readable form. Now one offer sourced from `RISK_REPORT`. (3) **The documented Mode B install could not work** — compose pulled a GHCR image nothing publishes while the workflow pushes to Docker Hub; README named env vars the server doesn't read (`LICENSE_KEY`/`PROXY_TARGET` vs `HOUNDSHIELD_LICENSE_KEY`/`UPSTREAM_API_KEY`) and an endpoint that doesn't exist (`/openai` vs `/v1`). Verified by booting the image and running the demo prompt: HTTP 403, "CAGE code (AC.L2-3.1.3)", 1 blocked event logged. (4) **`/api/brain/query` had no server-side CUI guard** — the warning was a label in `GlobalChat.tsx`, protecting nobody POSTing the route directly. Added a `classifyRisk` gate on the v3 (OpenRouter) branch only, failing closed; v1 is local BM25 and crosses no boundary. **Two broken gates found while verifying:** the CI pattern guard counted `grep -c "category:"` (matched the type alias + interface fields, would have passed with a dozen rules deleted) — now imports `ALL_PATTERNS`, floor 33 + CUI/PHI/PII coverage; `publish-patterns.yml` imported `PATTERNS`, a non-existent export, throwing on every run. And the proxy suite wrote its SQLite log into the working tree, so run 1 passed and every run after failed three tests **including "blocks the request when tool_call arguments contain CUI"** — real failures against accumulated OODA baselines, invisible in CI's fresh checkouts; `no-ignored-source.test.ts` had the mirror bug (flagged proxy `dist/` + `node_modules` as hidden source, so it only held while nobody followed our own quickstart). Gates: tsc clean both packages · lint 0 errors · **2220 app tests / 170 files green** · **61 proxy tests green over 5 consecutive runs** (was red from run 2) · next build passes · bench p99 **0.976 ms** vs 10 ms budget. → **NEXT: founder reviews PR #275 + the Vercel preview, then merges; production deploy stays an explicit separate go.** Reported but NOT fixed: no active pre-commit hook (`core.hooksPath` unset), no coverage gate (70% threshold sits in an orphaned `jest.config.js`), `lib/gateway` ~3,900 lines behind 161 lines of tests, two independent pattern registries (proxy 33 vs classifier 54) that can drift, stale `.claude/rules/stack.md` (says 4 migrations/16 patterns; 30/33 real), and `security-audit.yml` + `lighthouse.yml` both wrapped so they can never fail.

- [ ] 2026-08-08 (houndshield): **pitch deck rebuilt — founder rejected the first pptx.** Added the wordmark beside every mark in both formats (founder rule: the mark never appears alone) — HTML via a shared WORDMARK constant, pptx via a text object in the CONTENT master so it costs one stored object, not one per slide. Added 3 native, editable PowerPoint charts (addChart, not images) on the problem/liability/enforcement slides, each restating a number ALREADY cited on that slide: Netskope 89% vs 31%, MORSECORP SPRS +104 vs -142, DOJ cyber-FCA $15.6M vs $51.8M. Units live in dataLabelFormatCode so the axis scales on 15.6 while the label prints $15.6M. Added a section eyebrow to all 14 content slides — the fix for the real complaint, which was 13 identical bullet layouts. HTML charts are CSS divs (no library: a deck opens offline from an attachment); one zero-baseline code path handles the -142 bar. Verified: validator PASSED, 294 shapes 0 off-canvas, 0 off-palette across charts+slides+layouts, all 16 slides carry the name, chart bars measured proportionally correct in-browser. NOT COMMITTED per standing founder instruction. -> founder reviews the two files; approve to commit.

- [ ] 2026-08-07 (houndshield): **the after-login shell had ZERO responsive breakpoints and four pieces of fabricated chrome — premium pass across all 23 Command Center pages.** Founder asked for the default post-login dashboard to become premium, referencing `VoltAgent/awesome-design-md`, scoped (answered live) to all 23 pages with review before commit. **The panels were never the problem:** `OperatorOverview`/`OperatorPanels` already carried 13 breakpoints, already bridged to `--hs-*`, already honesty-gated. The failure was the shell above them. `grep -oE "\b(sm|md|lg|xl|2xl):"` over all 427 lines of `(tools)/layout.tsx` returned **nothing** — `fixed w-[260px]` sidebar + unconditional `ml-[260px]` main = **67px of usable content on a 375px phone, on every one of the 23 pages**, dismissable only via a "Collapse" button at the bottom of a 23-item nav that still left 68px. **Four fabricated-state defects, all live in production:** hardcoded `badge: "4"` on Quarantine (every customer saw a queue of four, empty ones included) · a static "All Systems Operational" pill with a pulsing dot checked against nothing · a notification bell with a permanent unread dot fed by nothing and opening nothing · `useState("K")` showing a Kaelus-era stranger's initial until `/api/me` answered. Plus a `<div>` search box advertising a ⌘K shortcut that did not exist. **The health pill could NOT be wired honestly** — `/api/health` returns `status: "healthy"` as a hardcoded literal, and its `services` block describes OUR vendor config (Stripe/Resend/OpenRouter), not the customer's posture — so it and the bell were deleted rather than faked harder; the ceiling and upgrade path are recorded as a `ponytail:` marker in `Topbar.tsx`. **Shell split** into `_shell/` (nav · Sidebar · Topbar · CommandPalette · useQuarantineCount) because 427 lines + drawer + palette breaks the 500-line rule; the ⌘K palette reads the SAME `NAV_ITEMS` the sidebar renders, so no second list can drift. **No `error.tsx` existed anywhere under `app/command-center/`** — a throw in any tool page took the whole shell down; now a boundary inside `(tools)` keeps the sidebar and offers Retry, plus a content-scoped `loading.tsx` (the existing one is full-viewport and blanked the sidebar on every tool-to-tool navigation). **Honesty pass:** reachability traced rather than assumed — only THREE pages a customer can reach render hardcoded data (`team`, `tasks`, `agents`); the other four mockups I expected are mounted by no page at all. New `SampleDataNotice` takes its wording from the existing `KIND_META.simulated` so sample data is described in one voice, and a **registry-not-heuristic** guard fails in both directions. **`DESIGN.md` was four months stale** — it described a dark `#07070b` indigo landing page that has not existed since light mode, on the exact artifact type the reference repo is about; rewritten on the awesome-design-md/Stitch spec from the real tokens, with a drift guard. **Guard tests moved from paths to invariants:** 11 assertions across 3 files pinned `(tools)/layout.tsx`, so the split would have failed 8 passing tests that were each still true — `helpers/shell-source.ts` now reads `layout.tsx` + `_shell/*` as one source, making this split and the next one free. Gates: **1992 tests green / 153 files** (from 1938/147, +54) · tsc 0 · lint 0 errors, 39 warnings all pre-existing with **zero in any touched file** (the "38" baseline in this file was stale). → **NEXT: founder reviews the diff, then approve → commit, push, draft PR; production deploy is a separate explicit go.**

- [ ] 2026-08-07 (houndshield): **restored `ponytail` for real, and wired TinyFish MCP into the repo config.** Follow-up to #267 (merged as `cdf95ad`), so this is a fresh branch off the new `main`, not a re-push. **Ponytail was never ours to reconstruct** — founder supplied the upstream, `DietrichGebert/ponytail` (MIT). Vendored verbatim at `.claude/skills/ponytail/` @ `16f2980` with `LICENSE` + `VENDORED.md` recording source, SHA and date, so local drift can't masquerade as upstream. Installed the core ladder plus `ponytail-review` (diff), `ponytail-audit` (repo-wide) and `ponytail-debt`; **skipped `ponytail-gain`** (a scoreboard of the vendor's own benchmark medians — publishing metrics we cannot verify is on the NEVER-DO list) and `ponytail-help` (modes now documented in Mode C). **The vendored skill confirmed the codebase was already following it:** rule 7 defines the `ponytail:` comment convention — ceiling + upgrade path — and `grep` finds **six live markers** across `proxy/ooda/loop.ts`, `lib/audit/seed-anchor.ts`, `app/api/dashboard/overview/route.ts` and `OperatorPanels.tsx`. `/ponytail-debt` now harvests them into a ledger instead of letting "later" become "never". **Mode C rewritten:** GATE 1 was 80% duplicated by the real ladder (YAGNI, reuse-before-write, fewer-lines, root-cause) — cut to the one thing ponytail does not cover, **blast radius**, per the skill's own "do not restate it, run it". Ponytail now owns *what gets built*; the four gates own blast radius, environment traps, delegation and irreversibility. **TinyFish MCP set up as Tier 1** of the WEB & RESEARCH ladder: `tinyfish` → `https://agent.tinyfish.ai/mcp`, `type: http`, added to the tracked `.mcp.json.example` and written to the gitignored local `.mcp.json`. No API key in either file — OAuth on first connect. Comment warns against double-registering via `claude mcp add` when it is already connected at account scope. → **NEXT: founder confirms `/ponytail` triggers on the next code task and runs `/ponytail-debt` on the six existing markers; `.claude/settings.json` hooks still point at `/Users/yantr/...` and cannot execute off the founder's laptop.**

- [ ] 2026-08-07 (houndshield): **ran the Six-Month Audit on `/houndshield` and absorbed the TinyFish web contract into the skill.** Audit fetched Anthropic's live Opus 5 + best-practices pages (never from memory) — 202 lines in, **3 deletes, 4 rewrites, 6 additions**. Deleted: GATE 2's "nothing is done without output pasted back" and GATE 3's re-verify-every-agent loop (Anthropic: *"remove them: instructions like these cause over-verification… do not use subagents to verify or double-check your own work"*), plus "MANDATORY"/"no exceptions" pressure language. **The GATE 2 delete was the audit's own false-positive trap** — it had four unrecoverable environment facts fused into it (piped exit codes, parent-config vitest, `--reporter=basic`, `.next` corruption); mandate framing deleted, all four facts kept under `REPO FACTS`. Four of seven delete checks came back **clean** (no anti-thinking rule, no XML ceremony, no vision workarounds, no severity filter). Added the five missing modern instructions (conciseness, deliverable length, narration cadence, task scope, subagent cap) + a `CALIBRATION` section. **New `WEB & RESEARCH` section** encodes the full TinyFish working model — 4-way router, 7-component goal anatomy, intern test, failure modes — over a 3-tier fallback ladder (TinyFish MCP → native WebSearch/WebFetch → local), so removing the MCP degrades capability instead of breaking it. Tier 3 is **`scripts/hound-web.mjs`**: zero npm installs, drives the on-disk Chromium over CDP with Node's native WebSocket; honours `HTTPS_PROXY` and derives SPKI pins from the trusted CA bundle so it works behind an interception proxy **without disabling TLS verification**. Verified live: `fetch`, `act` (eval/waitFor/scroll), `screenshot` all return real output. Also installed `~/.claude/skills/setup-auditor/SKILL.md` (reconstructed — the author's original is behind a Notion signed URL) and removed the stale `"model": "claude-sonnet-4-6"` pin from `.claude/settings.json`. **Two dead references found and made non-fatal:** `~/.claude/skills/ponytail/SKILL.md` and `~/.claude/primer.md` do not exist — Mode C routed ALL code work through ponytail, so every code request was hitting a missing file. → **NEXT: founder decides whether to restore `ponytail` (Mode C's code ladder) or let the four gates stand alone; separately, `.claude/settings.json` hooks still point at `/Users/yantr/...` macOS paths and cannot execute anywhere but the founder's laptop.**

- [ ] 2026-08-05 (houndshield): **closed the 34px mobile sideways-scroll flagged as a follow-up on 2026-07-30, found four more overflow defects while measuring, and un-blocked a RED `main`.** Founder reported `documentElement.scrollWidth` 409 vs `clientWidth` 375 on `/`, "not in the hero". Confirmed and root-caused in a real browser from COMPUTED STYLE: `.nav-inner` was correctly 375 but `.nav-cta` (239px) could not shrink — `.hermes .btn` is `white-space: nowrap`, which sets a min-content floor no available width can talk down. **Fix:** below 560px drop `.brand-text` (keep the mark — the logo's `alt="HoundShield"` still supplies the link's accessible name, so nothing is lost to a screen reader) and let `.hero-actions .btn` wrap. **Four more defects the founder had not reported, all found by measuring rather than reading class lists:** (a) the founder's stated premise was wrong in our favour — `.hero .container` reported `paddingLeft: 0px`, not 24px, at EVERY width, because `.hermes .hero-grid` used the `padding` shorthand on an element that also carries `.container`; same specificity (0,2,0), later rule wins, gutter silently zeroed. `origin/main` had independently landed the `padding-block` half of this; the missing half was `min-width: 0` on `.hero-grid > *` (grid items default to `min-width: auto`, so the nowrap CTA forced the track 379.3px wide inside a 375px viewport) — the same idiom `.docs-wrap > *` already used. (b) With the burger OPEN, `.dropdown.wide` (absolutely positioned, fixed 720px) ran from **-165 to 540** and dragged the document to **540px**; inside the stacked menu the flyouts are now in-flow accordions (`display: none` closed, not `visibility: hidden` — a hidden panel still occupies column height), re-shown on `:hover` **and `:focus-within`**, so the sub-nav stays reachable by keyboard. (c) The open menu used the nav's translucent `--nav-bg` at 82% alpha and the hero headline read straight through it — founder: "make it more visible it is hard too see" — now opaque `var(--page)` + `var(--shadow)`, 16px/600 links, 44px touch rows. (d) At **1200** the Products mega-menu rendered at **[-118, 588]** with its entire first column off-screen — a negative left edge never shows up in `scrollWidth`, which is exactly why the 375 sweep missed it; re-anchored with `.hermes .nav-links { position: relative }` + `.hermes .nav-item:has(> .dropdown.wide) { position: static }` → now **[91, 797]** inside 0–1194. (e) Founder: "reduce the space inbetween them so that logo and branding should look good" — measured every `.nav-item` carrying `padding: 8px 12px` + `gap: 12px` ON TOP of `.nav-link`'s own `8px 12px`. Root cause is a **cross-stylesheet class collision**: `app/globals.css:369` declares a bare, unscoped `.nav-item` for the DASHBOARD SIDEBAR, and it reaches the marketing nav because neither declaration is scoped. 24px dead space per item, ~120px across the row. Neutralised inside `.hermes` (scoping the sidebar class would touch the dashboard and its tests) → nav row **607 → 487px**. **Blocker found and cleared:** `origin/main` CI was **RED** — run `31000072671` on #262 failed in 49s because that dependabot bump took `stripe` to 22.4.0, whose `LatestApiVersion` moved to `2026-07-29.dahlia` while `lib/stripe/api-version.ts` still pinned `2026-06-24.dahlia`. Every PR cut from main inherited it, so this is a prerequisite, not scope creep; it is the exact one-file failure that file's own docstring was written to localise, and it ships as its own commit. **Verified after a real build, at three widths, on the built bundle:** `scrollWidth === clientWidth` at **375 (was 409) · 768 · 1200**, zero unclipped offenders at each, burger-open at 375 now **375 (was 540)**, `.hero .container` `paddingLeft: 24px`, and keyboard focus on "Products" at 768 opens the accordion in-flow (`display: block`, `position: static`, [13,749] inside 762). Guard: new `app/__tests__/mobile-overflow-contract.test.ts`, 14 tests — the `container`-companion check **re-derives its own target list from the JSX**, so a future `container foo-grid` is covered the day it is written, and the globals.css collision check is **self-retiring** (it fails, telling you to delete the workaround, the day someone scopes the sidebar rule properly). Gates: **1927 passed / 147 files** · tsc **0 source errors** · lint **0 errors** (39 pre-existing warnings) · build **EXIT 0**. → **NEXT: founder reviews the PR (it carries an unrelated but load-bearing one-line Stripe API-version bump that un-reds `main` — merging it fixes CI for every other open branch too), then confirms the nav on a real phone.**

- [ ] 2026-08-05 (houndshield): **The tamper-evidence pass the $499 report is sold on had never executed once.** `verifySeedChain`'s pass 2 ("Content integrity — re-computes SHA-256(content + previous_hash) … to detect tampering of log content") was guarded by `if (seed.content && …)` against a column that did not exist — `seed_anchors` carried no `content` from migration 001 through 029 and `createSeedAnchor` never wrote one, so every row skipped the check silently and the function still returned `{valid:true}`. Only pass 1 (linkage) ran: it detects a deleted/reordered **anchor** and is blind to an edited `compliance_events` row. **Option (b) from the brief — recompute from the source row, no new column — is impossible as stated, verified not assumed:** anchored EVENT content carries `timestamp: new Date().toISOString()` taken at anchor-build time, stored in no table (`compliance_events.created_at` is a separate `now()`), so the hash cannot be re-derived from the row, and changing the hash input would orphan every anchor (pinned by the "hash format compatibility" test). **Shipped (a)+(b):** migration `030_seed_anchor_content.sql` adds `content jsonb` (hash input UNCHANGED — written alongside, never into it); pass 2 now genuinely re-derives; new **pass 3** re-reads the live `compliance_events` row and compares `prompt_hash`/`risk_level`/`action_taken`/`classifications`, which is the pass that catches the actual attack. **Proved by mutation, not by a green suite:** reverting `content: data.content` fails 9 tests; leaving content in but neutering only the pass-3 comparison still fails 3 — so (a) alone would NOT have caught a downgraded `risk_level`. Legacy anchors are counted in `unverifiable` and reported, never folded into a pass. **Also fixed, unrelated and blocking:** `origin/main` was RED — dependabot #262 bumped stripe 22.3.2→22.4.0 twenty minutes earlier, moving `Stripe.LatestApiVersion`, and `lib/stripe/api-version.ts` was not followed through; CI runs `npx tsc --noEmit` (ci.yml:40) and `next.config` sets `ignoreBuildErrors: true`, so the build hid it. Gates: **tsc EXIT 0 · vitest EXIT 0 / 1924 passed / 146 files · lint EXIT 0 / 0 errors (39 pre-existing warnings) · build EXIT 0.** → **NEXT: `verifySeedChain` still has ZERO production callers — it is correct now but never runs, so the detection is capability, not practice. Wire it into the `integrity` block of `/api/reports/generate` (the section of the $499 PDF that already claims a merkle root, and where an assessor looks); that changes customer-facing report output, so it needs a founder yes. Migration 030 must also be applied to prod — run the 029 pre-flight queries first.**

- [ ] 2026-08-05 (houndshield): **the data rail was repaired but still unreachable — the URL the product printed was dead on TWO branded hosts, and a free-tier key was minted knowing the gateway would reject it.** Links 1–3 (mint → auth → write → read) shipped in `da61578`; they made the rail carriable, not reachable. **Probed against production, not inferred:** `proxy.houndshield.com` and `gateway.houndshield.com` both resolve to Vercel edge IPs and answer **404 on every path** — DNS is done, no project is attached — while `https://www.houndshield.com/api/v1/chat/completions` answers **401 Invalid API key**, which is the answer we want (route live, `resolveApiKey` reached the DB, failed closed). **Eight surfaces carried a dead host**, and fixing one host looked complete because the other was still there: Settings, the console settings tab, the public API docs, a marketing code block (`proxy.…`); five Brain AI answers, the chat system prompt, the runtime-modes table and the **day-3 onboarding email** (`gateway.…`) — that last one mails a paying customer an unrunnable URL 72 hours after signup. **Fixed by** `lib/gateway/base-url.ts`, one constant imported everywhere, plus a new **"link 0" guard** that walks all 626 `.ts`/`.tsx` files under `app/`/`components/`/`lib/`, strips comments (so the files that narrate the bug keep their history) **and strips backslashes** — the one instance that escaped my first sweep was `proxy\.houndshield\.com` inside a test's regex literal, and the suite caught it, not the guard. **Second cliff, same class:** `POST /api/gateway/keys` minted for any signed-in user while `POST /api/v1/chat/completions` rejects free tier with 402 (`canAccessGateway`), so a free user got a real, listed credential plus a curl snippet promising "the block lands on your dashboard as a real event within seconds" — and 402 forever. Issuance now consults the same predicate and returns 402 with the reason + `/pricing`. **Third, found while verifying:** `/v1/stats` and `/v1/events` were documented at the hosted base where they 404 — they are **not phantom**, they are `proxy/server.ts:167,175`, Mode-B only. `/v1/stats` takes no auth, which is correct on the customer's own single-tenant container and would have been a **cross-tenant volume leak** on the shared host. Both are now scoped to `PROXY_BASE` (`http://localhost:8080`, what the proxy prints on boot), badged "your proxy · Mode B" in the reference, and guarded so a no-auth endpoint can never be advertised on the shared host again. Also corrected: the Brain AI CUI answer names the FedRAMP status rather than the vendor (the FAQ leak guard bans naming the host to customers; CLAUDE.md bans implying the hosted rail is CUI-safe — naming the property satisfies both), and `/docs` step 1 no longer tells you to copy an admin token from a Settings page that issues gateway keys. Gates: **tsc EXIT 0 · vitest EXIT 0, 1918 passed / 145 files (was 1907/145) · eslint EXIT 0, 0 errors (39 pre-existing warnings) · build EXIT 0**. → **NEXT: FOUNDER — to ship a branded gateway host, attach `proxy.houndshield.com` (or `gateway.…`) to this Vercel project in the Vercel dashboard; the DNS already points there, only the project assignment is missing. Then one constant changes and all eight surfaces follow. Until then `www.houndshield.com/api/v1` is the only address that works, and it is what every surface now prints. Unchanged and still blocking revenue: `STRIPE_WEBHOOK_SECRET` is unset, so a $499 buyer pays and nobody is told.**

- [ ] 2026-08-03 (houndshield): **PR #255 merged ONE of six commits — five were silently orphaned, and three real defects are live in production right now.** Founder merged #255 at 2026-07-31T06:41Z, six minutes after CI went green on the FIRST commit (`f93c825`). A squash merge closes the PR, so every push to the same branch after that point stopped triggering PR checks and never reached `main`. That is the true explanation of the "GitHub Actions has not re-run" note in the two entries below — the runs were not late, the PR was already closed. **Verified, not assumed:** `git branch -r --contains 180e004` lists only the feature branch while `gh pr view 255` reads `MERGED`; `git diff origin/main 180e004` shows 22 files / 1,119 insertions still missing. **What that means is live on `main` today:** (a) `LiveCommandCenter.tsx:937` still ships `{isViewer ? 'Sample preview' : 'Live demo'}` **inverted** — every paying customer's real telemetry is captioned "Sample preview" in production; (b) `lccStyles.ts:576` still uses `column-count:2`, so the right-hand 7 NIST families render with no column headers; (c) `app/login/page.tsx` has no `isSignInAvailable` guard, so a deployment that can never sign anyone in still says "try again in a moment"; (d) the dashboard is still outside the `(tools)` sidebar. **Fix:** cherry-picked all five orphaned commits onto current `origin/main` as `HoundShield/dashboard-sidebar-and-login-fix`; `git diff 180e004 HEAD` is empty **unscoped**, so the tree is byte-identical to the already-verified head. Re-ran every gate on the replayed branch: **tsc EXIT 0 / 0 errors · vitest EXIT 0 / 1844 passed / 140 files · lint EXIT 0 / 0 errors (39 pre-existing warnings) · build EXIT 0** with `/command-center/overview`, `/plan`, `/guide` all present. **CI had never run on those five commits** — #256 was the first time, and it went **11/11 green** (the "Supabase Preview: skipping" row is a skip, not a failure: it only runs on `supabase/` changes). **SHIPPED: founder merged #256 as `eac1f68`.** Verified the way #255 taught us to — `git diff origin/main HEAD` empty **unscoped**, not just a MERGED badge — then confirmed production actually served it: `/command-center/plan` and `/guide` went **404 → 307** (307 = the auth gate wrapping a route that now exists; a 200 would have meant the gate was missing), with `/overview`, `/realtime` and `/shield/assessment` also 307. `grep -rn "Sample preview" components/ app/` now returns only comments and the tests asserting its ABSENCE. → **NEXT: founder hard-reloads `/command-center/overview` (Cmd-Shift-R — the old bundle is cached) and gives a verdict on the REAL dashboard; the three previous "still not the dashboard I asked for" rounds were all verdicts on code that had never deployed. Expect the KPI tiles to still be EMPTY — that is `useOperatorTelemetry.ts:15` ("None of the three has a seeded fallback") doing its job, not a missed fix. The open, UNDECIDED product question is what a signed-in customer with zero traffic should see: (2) an opt-in "Preview with sample data" button, or (3) sample-by-default with heavy labelling — (3) reverses a hard CLAUDE.md rule and the guard at `operator-dashboard-honesty.test.ts:90`, so it must never be built on inferred consent. Separately: add `NEXT_PUBLIC_SUPABASE_ANON_KEY` to the Vercel PREVIEW environment (needs a REBUILD — `NEXT_PUBLIC_*` is inlined at build time), or previews keep saying "Sign-in is not configured on this deployment", which is the new error message working correctly.**

- [ ] 2026-07-31 (houndshield): **founder could not sign in to the Vercel preview — root-caused to a missing PREVIEW env var, and the misleading error fixed in code.** Login showed "We couldn't reach the sign-in service. Please try again in a moment." — advice that cannot come true. **Root cause is deployment config, not code:** the Preview environment has `NEXT_PUBLIC_SUPABASE_URL` but NOT `NEXT_PUBLIC_SUPABASE_ANON_KEY`. `NEXT_PUBLIC_*` is inlined at BUILD time, so the difference is provable in the shipped bundles — prod compiled the key to a string literal, preview compiled it to a `process.env` lookup that is `undefined` in a browser. `createBrowserClient(url,'')` throws synchronously; every sign-in path caught that throw and reported it as a transient network blip. Same reason preview `/api/health` says `database: demo_mode` while prod says `connected` (`isSupabaseConfigured()` requires an anon key >20 chars). **Fixed in code:** new `lib/auth/signin-availability.ts` — all three entry points (password, passwordless code/link, OAuth) now ask "can this deployment sign anyone in?" BEFORE trying, and say so plainly. OAuth needed it most: there the throw was inside an async handler with NO catch, so the rejection went unhandled and the button simply read as dead. The generic catch stays — a real network failure IS transient and "try again" is honest for that. **The env var itself is founder-only (GATE 4 — credentials).** The anon key is public by design (it ships in the prod browser bundle), so adding it to Preview exposes no secret; the real tradeoff is that preview would then authenticate against the PRODUCTION Supabase project, i.e. preview code could mutate prod data. Gates: tsc 0 - 1844 tests green (140 files) - lint 0 errors - build clean. **This commit did NOT ship in #255 — see the 2026-08-03 entry; it is re-opened as a fresh PR off current `main`.** -> **NEXT: founder adds `NEXT_PUBLIC_SUPABASE_ANON_KEY` to Vercel Preview (Settings -> Environment Variables -> tick Preview) and redeploys. Until then the preview will correctly say "Sign-in is not configured on this deployment" — that message IS this fix working, not a regression.**

- [ ] 2026-07-31 (houndshield): **the dashboard was the ONE page that escaped the Command Center sidebar — now it lives inside it, with real data.** Founder sent screenshots of the 23-item sidebar: "I still want all of these features and the dashboard home section should look like these." Root cause: `LiveCommandCenter` ships its own `hs-lcc` shell with a rival 10-item nav, so "Dashboard Home" and "Overview" were links that navigated OUT of the navigation and the 20 deep tool pages were unreachable from the page a customer lands on. **Fix:** `app/command-center/overview/page.tsx` moved into the `(tools)` route group — parentheses-only, so `/command-center/overview` is byte-for-byte the same URL and the 7 post-login landings + `/login?redirect=` + every bookmark keep working. New `OperatorDashboard` carries the `.hs-lcc` wrapper (every panel rule is scoped to that class) but NOT `.hs-lcc .shell` (the 248px sidebar grid) — that split is what lets the stylesheet travel without painting a second sidebar; `hs-embedded` kills the fixed-attachment page gradient. **Panels unchanged**: same `OperatorOverview`, same three real sources (own gateway events via `/api/dashboard/overview`, own on-device 110-control assessment, own recorded posture history). **Nothing stranded:** moving out of the tab shell would have silently deleted every pane that existed only as a tab — including **Plan & Unlocks, the only upgrade surface in the product**, a revenue path a refactor nearly removed; both rescued as routes + sidebar entries (`/command-center/plan`, `/command-center/guide`) and a new guard walks EVERY sidebar href and fails if any lacks a page. **Three live defects fixed:** (a) the hero read `isViewer ? 'Sample preview' : 'Live demo'` — inverted, so every paying customer's real telemetry was labelled "Sample preview" while the seeded demo read "Live"; (b) the activation checklist passed a CONSTANT, telling every operator "point your traffic at the proxy" and "see your first live scan" were done — including those who had never sent a prompt — now a real per-tenant count (`hasGatewayTraffic`, session-derived `user_id`, fails CLOSED); (c) `.op-matrix` used `column-count:2`, which puts the single header atop the LEFT column only, leaving the right-hand 7 NIST families headerless — found by rendering in a real browser and reading COMPUTED STYLE, not the class list. Guards that encoded the old architecture were rewritten (not deleted) with the reversal recorded: the dashboard now sits INSIDE (tools), NOTHING may escape the tool shell, and the mockup check reads the file at the path instead of asserting the path is empty. Gates: **1834 tests green (139 files)** · tsc 0 · lint 0 errors (38 warnings, DOWN from 49) · build clean with `/command-center/overview`, `/plan`, `/guide` all `ƒ` Dynamic · Vercel preview deployed on `e971243`. **None of this shipped in #255 — see the 2026-08-03 entry; re-opened as a fresh PR off current `main`.** → **NEXT: founder opens the new preview, confirms the sidebar + dashboard match the screenshots, and authorizes the merge on a green CI rollup.**

- [ ] 2026-07-30 (houndshield): **the hero sat flush against the left window edge, and the "fix" appeared not to work because the preview was serving a stale build.** `.hermes .container` supplies the 24px side gutter, but `.hermes .hero-grid` then set `padding: 72px 0 84px` — both are specificity (0,2,0), the shorthand writes all four sides, and the later rule won, zeroing the gutter. At a 1200px viewport (= `--maxw`) there is no auto margin left to absorb it. Fixed with `padding-block` (both the base rule and the mobile breakpoint) so the inline-axis gutter survives. First re-measurement still read `padLeft: 0px` — NOT a losing cascade: the running preview was `houndshield-prod` (`npm run start`), serving a `.next` built before the edit, and the reading matched the OLD shorthand's output byte-for-byte. After rebuild: **`paddingLeft: 24px`, `h1Left: 24`, no horizontal overflow at 1200px and at mobile (`padTop 48 / padBottom 56`).** Founder then asked for two hero changes: (a) name Claude and Gemini alongside ChatGPT **"if it is correct"** — verified against `proxy/server.ts:51` (`type Provider = "openai" | "anthropic" | "google" | "openrouter"` with live `api.anthropic.com` and `generativelanguage.googleapis.com` endpoints), so the claim is true and the h1 now reads "…pasted into ChatGPT, Claude and Gemini."; (b) replace the demo dashboard's Gateway-throughput line chart with the Live prompt scans feed **at the same size** — done, and measured: both cards in that row are **196px tall**, exactly matched. The old full-width feed strip at the bottom is gone, `FEED` dropped 5 → 3 so the feed occupies the 84px the chart did, rows recessed to `--a-win` since they now sit inside a white `.hd-card`, and all dead chart code removed (`series`/`N`/`W`/`H`/`MAX`/`pts`/`line`/`area`/`lx`/`ly`, `.hd-line`, `.hd-grid-l`, `.feed-h`, plus the stale header comment). Gates: **1613 tests green** (129 files) · proxy **61/61** · tsc 0 · lint 0 errors / 39 warnings · build EXIT 0 · `verify:structure` PASS. Two verification traps logged to lessons.md: the proxy suite is red LOCALLY ONLY because a gitignored `proxy/data/houndshield-events.db` holds accumulated LOCKOUT state that the DB module reads at import time, before `beforeEach` redirects `HOUNDSHIELD_DATA_DIR` (moving it aside → 61/61, and CI on main is green across four runs); and `cmd > log 2>&1; echo "EXIT=$?"` reports the echo's status to the task notifier, not the command's. Found but NOT fixed here (pre-existing, outside `.hero`, flagged as its own task): `.nav-cta` overflows `.nav-inner` by 34px at 375px, giving the homepage sideways scroll on mobile. → **NEXT: founder reviews/merges the PR; then Vercel Root Directory = `compliance-firewall-agent` + delete the root `vercel.json` together (docs/DEPLOYMENT-MIDDLEWARE.md), and apply migration 028 so the shared rate limiter stops silently falling back to per-instance.**


- [ ] 2026-07-29 (houndshield): **shipped the CI control that makes the identity leak un-repeatable, and drafted the four real outreach emails into Gmail.** Founder asked for the emails drafted "with genuine emails". **Control first:** the #251/#252 leak was caught by a human reading a diff, and `docs/SECURITY-ROTATION.md` records the same class of failure with teeth (an older project's `backend/.env` with a live Mongo URL + JWT secret + LLM key, deleted 6 days later and **still readable in history**). New `scripts/verify-no-leaks.mjs` + CI job **Secret & PII Guard** (green in CI, not just locally) fails on 12 credential families, tracked `.env` files, and consumer-domain personal addresses. Three deliberate design calls: scans only `git ls-files` (that IS the threat model — what's tracked is what's published, and node_modules/.next fall out for free); **allowlists VALUES not PATHS** (skipping `__tests__/` is how scanners get defanged — the next real key in a test file would sail through); and discriminates on **shape** — prefix-only would flag `re_pageview`/`re_patterns`/`re_evidence` and every inline sourcemap in `_bootstrap.html`, so candidates must clear a length floor + repeated-run + sequential-run + character-mix + Shannon entropy ≥3.2, a JWT must decode to a JOSE header, and a private key must have a base64 body (because quoting these shapes is what `proxy/PATTERNS.md` does for a living). `--self-test` (14 must-flag / 27 must-pass) runs in CI ahead of the scan and **caught the guard being wrong 3 times before it ever ran on a PR** — `AKIA1234567890ABCD12`, 7 docker-compose URLs, and the PATTERNS.md header; those 3 rules exist only because of it. **GitHub Push Protection then rejected my own push**, reading two self-test fixtures as a live Stripe key and a live Supabase token — fixtures are now assembled from split fragments (a test fixture must not be able to block a push, and clicking an "allow this secret" unblock URL trains exactly the wrong reflex), and the email fixtures are synthetic because using the real founder Gmail would reintroduce the leak being removed. Guard then found **4 live hits, scrubbed**: the founder's personal Gmail in `CLAUDE-CODE-PROMPT.md` ×2 + `CLAUDE-CODE-ULTIMATE-PROMPT.md`, and an internal demo account in this file. **Emails:** 4 Gmail drafts created via Gmail MCP, rendered through the real `render()` code path with identity supplied as env (never committed) — smoke-test is send-ready to the founder mailbox; the 3 outreach ones are addressed to the founder's OWN inbox with a `TEMPLATE (…)` subject prefix and a fill-in header, so an accidental send reaches the founder, never a buyer. **Two copy defects found by reading the rendered drafts in the actual mail client** rather than trusting the source: the defense draft told a DoD security manager to click **"Patient Record"** (now audience-specific — healthcare/`Patient Record`, partner/`AWS Config`, defense/`Network Scan`, guarded against `app/demo/page.tsx` so renaming a demo button fails the suite), and the scheme-less `houndshield.com/demo` was rewritten by Gmail into a `google.com/url?q=http://…` tracking redirect over **HTTP** (now explicit `https://`, guarded by a negative lookbehind). Both guards **proven to discriminate** (reverting each turns them red). Also shipped: `/roi` no longer denominates against "HoundShield Pro at $799/mo" — a tier deleted from /pricing in #243 — renamed to `MODELED_*` and labelled as a modeling assumption; deliberately NOT re-denominated to $499, because 12 months ÷ $499 yields a ~600x "ROI" that would destroy credibility with the buyer the page exists to persuade. **Verified: 1692 passed / 131 files · tsc 0 · lint 0 errors (40 pre-existing) · build EXIT 0 · leak guard clean.** Docs: `docs/LEAK-GUARD.md`. PR **#253**. → **next: FOUNDER — (a) in Gmail set the founder's own `@houndshield.com` mailbox as the DEFAULT "send mail as" identity and change its display name off "HoundShield" to the founder's own name (the screenshot shows it reading "HoundShield", which is the newsletter failure mode the drafts are written to avoid); (b) send the smoke-test draft and DO step 4, the reply test; (c) with Gmail/Hostinger SMTP *and* Resend both sending for the domain, the single-merged-SPF-record requirement is now live — two `v=spf1` TXT records = `permerror` = everything to spam; (d) `STRIPE_WEBHOOK_SECRET` still unset; (e) still awaiting the GATE-4 decision on the git-HISTORY rewrite + credential rotation.**

- [ ] 2026-07-29 (houndshield): **removed the founder's personal identity from a PUBLIC repo — my own mistake from PR #251, caught by the founder.** In #251 I hardcoded the founder mailbox as `export const FOUNDER_ADDRESS` and propagated it into 2 docs, 2 test files, both task logs, the skill file, the commit message and the PR body — then merged it. Repo visibility confirmed via the GitHub API: **`"visibility": "public"`**. A personal work mailbox *and* a personal Gmail (in a historical todo entry) were sitting in merged `main`, indexed. **Fix:** identity is now env-only — `founderName()` ← `FOUNDER_NAME`, `founderAddress()` ← `FOUNDER_EMAIL`, both read at call time, with in-repo defaults that are safe to publish (`HoundShield` / the already-published `contact@houndshield.com`). Unconfigured = impersonal, never leaked. `founder-access.ts` went the opposite direction on purpose — **fail-CLOSED**: with no env set the founder-override list is empty, because a missing var must never silently grant top-tier access; new `isFounderAccessConfigured()` makes that visible. `identity.ts` is also now the ONLY reader of `FOUNDER_EMAIL` (via `configuredFounderEmail()`), so mail routing and the access grant cannot disagree. Scrubbed ~20 committed occurrences across source, tests, 5 docs, the skill and both task logs. **New guard, split by surface** (the broad version wrongly flagged `security.txt`/`privacy`/`dpa`, which must publish a contact): API routes + lib modules may hold NO mailbox literal, and *any* page may only publish a **generic** local part (contact/info/security/privacy/…) — so a re-introduced personal address fails even in prose. **Proven to discriminate:** injecting `somepersonsname@houndshield.com` turns it red; removing it green. **Verified: 1684 passed / 131 files · tsc 0 · lint 0 errors (40 pre-existing) · build EXIT 0.** Send tool re-verified both ways: unconfigured → `HoundShield <contact@houndshield.com>`; with env → the real name + mailbox. Two lessons logged (disclosure-vs-correctness; blanket-regex-mangles-prose). → **next: FOUNDER DECISION — the address is still in git HISTORY (merged commit `d5739e2` + 9 pre-existing test files predating this session). Removing it needs a history rewrite + force-push, which I will NOT do without your explicit yes; `docs/SECURITY-ROTATION.md` already lists 4 leaked creds to rotate BEFORE any rewrite, so these should be done in one pass. Alternative: accept the history and keep HEAD clean. Also still open: set `FOUNDER_NAME` + `FOUNDER_EMAIL` in Vercel Production (otherwise outreach signs as "HoundShield" and the founder console override is off), and `STRIPE_WEBHOOK_SECRET` remains unset.**

- [ ] 2026-07-29 (houndshield): **the founder email identity was a rule in a skill file that no code enforced — and `/demo` was selling a product that does not exist.** Founder asked to draft outreach from the founder mailbox via Hostinger MCP, with a non-IT step-by-step test guide in the email. **Two things had to be corrected before building:** (1) **there is no Hostinger MCP in this session** — searched the deferred-tool registry for `hostinger` (0 matches), no `HOSTINGER_*` token in env, and container egress is 403-blocked at the gateway (`developers.hostinger.com` CONNECT rejected), so the mailbox could not be provisioned or read programmatically; live prod state was instead read through TinyFish server-side. (2) The repo recorded "only `info@` exists" in two places (`SKILL.md:24`, `OUTREACH-HEALTHCARE.md:6`) from a single un-re-verified `/api/v1/me` read — **founder confirmed all three of the founder mailbox/`contact@`/`info@` exist**, so sale alerts were never bouncing; both stale records corrected. **The real defect:** four routes each resolved "the inbox a human must act on" independently and two disagreed — `$499 sale alert`/`contact form`/`snapshot lead` → `contact@`, but **`partners/apply` (the Stage-1 0/1 RPO goal) → `info@`**; `FOUNDER_EMAIL` is unset in prod so those fallbacks were live. Worse, `/api/contact` and `/api/report/snapshot-lead` returned their **routing** address to the browser as `fallbackEmail`, so pointing `FOUNDER_EMAIL` at a private mailbox would have published it to every visitor during a Resend outage. Nine files hardcoded their own `From`. **Fix:** new `lib/email/identity.ts` as the single source (routing vs published address separated by design; `founderInbox()` defaults to the founder mailbox; a malformed `FOUNDER_EMAIL` is **ignored, not obeyed**, and surfaced by a new value-free `founder_inbox`/`founder_inbox_domain` on `/api/health` that reports domain+source only, never a mailbox); all 4 routes + 8 templates + `auth-emails` wired to it; `founder-access.ts` now imports the founder address instead of keeping a second copy. **`lib/email/outreach.ts`** holds 4 drafts (smoke-test/healthcare/partner/defense) where `render()` **throws** rather than emailing `Hi [First name]`, each buyer draft carrying a 5-step jargon-free test guide whose last step lets a non-technical buyer **prove local-only by turning off Wi-Fi and scanning again**. **`npm run email:preview`/`email:send`** (tsx): dry-run default, ONE recipient per run (no bulk cannon — this domain also carries password resets), refuses reserved/placeholder addresses; all three refusals verified exit 1. **`/demo` honesty fix (in scope because the email links there):** removed 13× "HoundShield Pro" (a tier deleted from /pricing in #243, with "Get Full Protection" pointing at `/auth`), the false **"1-click SOC 2"** claim (SOC 2 not started), and the fabricated **"13 AI models"** — there is no model in the scan path at all; it contradicted the local-regex moat. Now quotes the real 16 engines/90 patterns and funnels to `/assessment`. **Verified: 1678 passed / 131 files (was 1583) · tsc 0 · lint 0 errors (40 pre-existing) · build EXIT 0, 226 pages.** New source-level guard `email-identity-single-source.test.ts` **proven to fail against the original bug** (restored the old `info@` fallback → 3 assertions red), and it caught two of my own violations mid-build (a 9th hardcoded `From` in `api/email/welcome`, and my own direct `process.env.FOUNDER_EMAIL` read in the health route). Proxy untouched → proxy suite/bench not run. Docs: `docs/FOUNDER-EMAIL-IDENTITY.md` (incl. the Resend-sends/Hostinger-receives split and the single-SPF-record `permerror` trap, values taken from Hostinger's own doc) + `docs/OUTREACH-SOURCING-RUNBOOK.md`. → **next: FOUNDER — (a) run the smoke test to a mailbox you own (`npm run email:send -- --template smoke-test --to you@… --confirm`) and DO step 4, the reply test, since sending and receiving are separate systems; (b) `STRIPE_WEBHOOK_SECRET` is STILL unset — live-verified again this session, and it alone turns on order recording + sale alerts; (c) source 10 genuine healthcare recipients per the runbook and send. Known remaining orphan I did NOT touch: `components/dashboard/roi/RoiDashboard.tsx:149` still prices ROI against "HoundShield Pro at $PRO_MONTHLY/mo".**
- [ ] 2026-07-29 (houndshield): **the dashboard at `/command-center/overview` was an 804-line mockup with no session lookup** — that is why it "showed something else logged in". Founder asked for it to become the default after-login dashboard. Every chart in it was hardcoded (`generateTokenData`, `threatDistribution`, `riskRadarData`, `providerBreakdown`, `recentEvents`, plus a `REVENUE_DATA` block that was HoundShield's revenue, not the customer's), and the file contained no `getSessionProfile`/`useSession`/`fetch` anywhere — so every signed-in operator saw the same invented security metrics and could not be greeted by name. Making that the default post-login surface would have shipped fabricated metrics as each customer's own data (NEVER-DO) on a product sold as audit evidence. Founder chose the honest option: **deleted the mockup and moved the REAL dashboard onto its URL.** `LiveCommandCenter` already had all five features the founder pointed at (Live Threat Feed, CMMC Assessment, Reports, Brain AI, account menu) AND was already honesty-gated — signed-in operators get a stripped Overview with simulated telemetry hidden, per founder direction 2026-07-23; only the anonymous demo shows the "Acme Defense" sample, labelled `sample data`. It sits OUTSIDE the `(tools)` group on purpose: that group's layout paints a full `cc-light` shell (aside+header+main) and LiveCommandCenter brings its own `hs-lcc` shell, so nesting = two sidebars. `/command-center` now forwards to it (temporary, not 308 — a permanent redirect would cache forever and strand every visitor if the URL ever moves), so all seven post-login landings and every bookmark keep working with a one-file change. Also: **`⌘ v2.0 BEAST MODE` in the tools header → the signed-in customer's company**, read from the existing `/api/me` (which already returned `company`) rather than re-deriving `profileKeyColumn()`'s Better-Auth-vs-Supabase column choice in the browser; renders NOTHING when no company is set, because a stand-in org name is fabricated data too. Removed LiveCommandCenter's self-referential "All tools → /command-center/overview" link (it pointed at the page you were already on). Gates: **1613 tests green** (129 files, +8 new) · tsc 0 source errors · lint 0 errors / 39 warnings (one BELOW the 40 baseline) · build clean, every `/command-center/*` route `ƒ` Dynamic so nothing prerenders to the CDN. → **NEXT: founder merges the PR, then set Root Directory to `compliance-firewall-agent` + delete root `vercel.json` together (docs/DEPLOYMENT-MIDDLEWARE.md) to bring middleware back.**

- [ ] 2026-07-29 (houndshield): **the dashboard was PUBLIC in production, and the middleware meant to stop it had never run.** Founder asked to merge the after-login dashboard into `/command-center` and put it behind login. Probed prod first instead of reading the code: `curl https://www.houndshield.com/command-center` → **200** with `x-nextjs-prerender: 1` + `x-vercel-cache: HIT` — the whole 20-page Command Center served to anonymous visitors from the CDN. `middleware.ts` had the correct guard and **compiled** (`ƒ Proxy (Middleware)` in the prod build log) but was not executing: no `X-Robots-Tag`, no `X-RateLimit-*`, `/auth/signup` → 404 instead of redirecting. Four independent signals, one cause — **the repo-root `vercel.json` still uses the legacy `version:2` + `builds` + `routes` keys** ("WARNING! Due to `builds` existing in your configuration file…" in the build log), and the legacy `routes` array REPLACES the routing table Vercel generates from the build, which is where the middleware route lives. Same blast radius hit `next.config.js` `redirects()` — `/dashboard` and `/shieldready` both **404 in prod** — and `compliance-firewall-agent/vercel.json`'s cron (`/api/cron/email-drip` has never fired). Rewrites + headers survive (Next serves those itself), which is why `/hermes` works. **Fix, deployment-independent:** a fail-closed SERVER layout at `app/command-center/layout.tsx` (`getSessionUser()` → `redirect('/login?…')`, no try/catch, `force-dynamic`) — every `/command-center/*` route moved `○ Static` → **ƒ Dynamic** in the route table, so there is no prerendered HTML left for the CDN to serve. **Second bug found while verifying:** with middleware disabled to simulate prod, the gate answered **200, not 307** — a root `app/loading.tsx` (untouched initial-commit scaffold) wrapped the ENTIRE app in one Suspense boundary, and Next delivers `redirect()` from a streaming render as a meta tag, not a status code. Moved it to `app/command-center/loading.tsx` (below the gate) → real 307s; that also un-masked a latent bug the root boundary had been hiding, `/login` calling `useSearchParams()` with no Suspense of its own, now fixed so `/login` stays prerendered. **Merge:** `/command-center` is the one canonical dashboard and its index now renders the LiveCommandCenter; the 20 deep tools moved into a `(tools)` route group (URLs unchanged, no double sidebar); `/console`, `/console/security`, `/dashboard`, `/shieldready/*` are in-app `permanentRedirect` routes (immune to the platform config); every post-login landing repointed; the console sidebar gained real links into the tools (it was an island); the tool shell gained a sign-out. NavV3's public "Live Threat Dashboard" flyout repointed `/console` → `/demo` so prospects don't hit a login wall. **PROVEN with middleware deleted and rebuilt — exactly the production condition: all 7 `/command-center/*` probes 307 → `/login`, `/console` 308, public pages 200.** +24 tests → **1583 passed** (128 files) · tsc 0 real errors · lint 0 errors (40 pre-existing) · build EXIT 0. Doc: `docs/DASHBOARD-AUTH-GATE.md`. → **next: FOUNDER — (a) merge this PR (closes the public dashboard immediately, no deploy config needed); (b) Vercel → Settings → Root Directory = `compliance-firewall-agent`, THEN delete the repo-root `vercel.json` (in that order) to revive middleware, rate limiting, the config redirects and the cron; (c) `STRIPE_WEBHOOK_SECRET` is STILL unset — live-probed `POST /api/stripe/webhook` → 503 "not configured"; the var added as `Stripe_singing_secrate` is not read by anything, the name must be exactly `STRIPE_WEBHOOK_SECRET`.**

- [ ] 2026-07-29 (houndshield): **the FAQ was not "off-centre on /pricing" — an unlayered CSS reset was killing every Tailwind spacing utility inside `.hermes`.** Founder reported the `/pricing` FAQ sitting left. Measured rather than read: the section computed `margin-inline: 0` and `padding-inline: 0` while `max-w-3xl` still applied. Root cause in `app/hermes.css` — `.hermes *, ::before, ::after { margin:0; padding:0 }` was **unlayered**, and Tailwind v3 emits no cascade layer, so the reset and every `.px-5`/`.mx-auto` utility tied at specificity (0,1,0), broken by source order because `layout.tsx` imports `globals.css` before `hermes.css`. The reset won every tie. Browser audit before the fix: **236 dead spacing utilities on `/faq`** (the search input's `pl-12 pr-12` — its icon overlapped the placeholder — every category pill, all 38 accordion rows) and **36 on `/pricing`**. `max-w-*` masked the whole class by still applying (the reset never sets `max-width`), so boxes were the right width in the wrong place with contents flattened. Fix: wrap the reset in `@layer hermes-reset` — layered rules lose to ALL unlayered author rules regardless of specificity, so utilities win, while UA defaults still lose to the reset and Tailwind Preflight (unlayered, already loaded) keeps zeroing heading/list margins; hermes's own rules stay unlayered and still outrank it. Verified after rebuild at 1440px: **0 genuinely dead utilities** on `/faq`, `/pricing`, `/docs`, `/partners`, `/how-it-works`; FAQ section now **216/216** inside its 1200px container (was 24/408); hermes-native untouched (`.step` 26×28, `.card` 26×26, `.container`/`.nav-inner` 0/24); 0 console errors. Founder also asked for the FAQ rows "bit bold and more spacious, like these [.step rows]" → `FaqAccordion` question 600→**700** weight and 1.05rem→**1.18rem** (exactly `.step h3`), row padding 20px→24/28px, row gap 12→16px (`.steps` gap), looser answer leading. Also in this PR: **`NavV3`'s Pricing flyout quoted four monthly tiers (Free/$199/$499/$999) on EVERY page**, each row linking to a `/pricing` that sells none of them; two guards contradicted each other and the stale one won (`pricing-single-offer.test.tsx` `vi.mock`s `NavV3` away, so it certified a page that does not ship). Nav now quotes only the $499 report and $299 wholesale from `RISK_REPORT`; `about` dropped "$49/mo" + "14-day free trial" (assembled at render time from prefix+value+suffix, so no source grep could find them); `offer-honesty.test.ts` widened 3 paths → **268 files** with a self-auditing allowlist. **1552 passed** (was 1549) · tsc 0 · lint 0 errors · build clean · `@layer hermes-reset{` confirmed in the minified bundle. Shipped as **PR #248**. → **next: founder — merge PR #248 once CI is green (auto-deploys prod); then set `STRIPE_WEBHOOK_SECRET` in Vercel Production, which alone turns on order recording + sale alerts (see the 2026-07-29 entry below).**

- [ ] 2026-07-28 (houndshield): **🔴 THE COMPASS WAS WRONG — CMMC Phase 2 was SUSPENDED 2026-07-13, and we did not know.** Full diagnosis + pivot shipped as **PR #243**. (1) **The market event:** the Department of War suspended CMMC Phase 2 (the 10 Nov 2026 C3PAO gate is GONE, Phases 3–4 frozen, 60-day review ends ≈11 Sep). `primer.md` said "confirmed unchanged 2026-07-12" — stale by ONE DAY on the single most important external fact in the business. **What survived:** DFARS 7012, the 110 NIST 800-171 Rev 2 controls, and annual SPRS self-attestation. **The new wedge is FCA liability** — with no assessor in the loop the SPRS score is the contractor's own representation, and DOJ has settled 15 False Claims Act cases (MORSECORP $4.6M, LOGZONE $507,144). **Lead buyer is now HEALTHCARE** (Netskope 2025: 89% of healthcare genAI violations involve regulated data vs 31% cross-industry; 43% use personal genAI accounts at work) — no deadline dependency, no FedRAMP blocker. (2) **Revenue path unblocked in code:** `/pricing` sold BOTH the $499 one-time report AND a "Growth $499/mo" tier (there was literally an FAQ apologising for the confusion) → collapsed to one offer, and the route is now a Server Component; the global nav's "Start free → /signup" pointed at a tier that no longer exists → both navs now read `PURCHASABLE_OFFER`; the console told locked users to "upgrade to Growth" → same single source. (3) **Honesty:** "16 detection engines" was CORRECT but hardcoded in 4 places → now computed in `lib/detection/engines.ts` (16 engines / 90 patterns); replaced the unsourced "~80,000 contractors" with the Netskope figure; FAQs said "C3PAO assessment required — Yes", **false since 13 July and being fed to Google as FAQPage schema by a compliance vendor** → corrected. (4) **FAQ style unified** (founder request): `/contact` hand-rolled its heading + JSON-LD → now the shared `FaqSection`, one surface everywhere. (5) **Proxy 17 failed → 61 passing with ZERO code changes** — root cause was `NODE_MODULE_VERSION 137 vs 127`, a stale `better-sqlite3` binary; `npm rebuild` fixed it. This had been logged as "pre-existing CI breakage" for weeks. (6) **Repo 6,267 → 1,841 tracked files (−71%, −90MB)** — `legacy/` (43MB, zero imports, tsconfig-excluded) + `brain/` (47MB dupe) moved to `~/HoundShield-Local-Archive/` and gitignored; kept `agents|skills|commands|rules` because `verify-structure.mjs` pins them and `agents/agentharness` bridges a live submodule. (7) **`/houndshield` gained the half it never had** — Mode C routes all code work to the existing `ponytail` skill + 4 gates (think-before-writing · verify-before-done · **review every agent's output** · ask-before-irreversible); war room capped at 4 stages; new portable founder-CEO skill (kept outside this repo) for other projects. (8) **Docs:** STRIPE-FIX · TESTING-GUIDE (from zero, incl. be-your-own-customer) · ROADMAP-12-MONTH · SECURITY-ROTATION · OUTREACH-HEALTHCARE (30 drafts, **none sent**). **Verified: app 1531/1531 · proxy 61/61 · bench p99 0.492ms (20× under the 10ms claim) · build 253 pages · tsc 0 · structure guard PASS.** Browser-verified `/pricing` + `/contact`. → **next: FOUNDER — (a) paste the `sk_live_` secret into Vercel Production (`/api/health` still `malformed_key`; NOTHING earns a dollar until this is done), (b) add `STRIPE_WEBHOOK_SECRET`, (c) add `DOCKERHUB_USERNAME`/`TOKEN` + push a `proxy-v1.0.0` tag (the publish workflow is already correct — it has never had credentials or a tag), (d) rotate the 4 leaked creds in git history BEFORE any rewrite (`docs/SECURITY-ROTATION.md`), (e) create the founder mailbox (only `info@` exists), (f) SEND THE 30 EMAILS — nothing else matters as much.**
- [ ] 2026-07-29 (houndshield): **closed the silent-sale hole — a $499 buyer could pay and nobody would ever know.** Traced the money path instead of trusting the briefing: `/api/health` reads `payments: missing_key` + `payments_webhook: missing_secret`, which LOOKS like dead checkout, but a live prod curl proved retail still sells — `/api/stripe/report-checkout` returns the Stripe-hosted Payment Link fallback (`rail: payment_link`, verified 200 against production). The storefront was open; the till was not wired. `POST /api/stripe/webhook` opened with `if (!getStripeSecretKey() || !webhookSecret) → 503`, so a real Payment-Link sale was answered with 503: **no `report_orders` row, no buyer receipt, no founder alert.** Root cause: `webhooks.constructEvent` is a LOCAL HMAC check needing only the signing secret, and `handleReportOrder` never calls Stripe — grep proved exactly ONE Stripe API call exists in the whole route (`subscriptions.retrieve`, line 225). So the key gated a path that never used it. Fixes: (1) gate requires `STRIPE_WEBHOOK_SECRET` alone → that one variable now makes the money path whole; (2) subscription events without a key **acknowledge with 2xx** (a non-2xx makes Stripe retry → disable the endpoint → would take the report path down with it); (3) `getStripe(secretKey)` takes the key explicitly with a non-`sk_`-shaped placeholder (stripe-node 22.x throws on a falsy key — verified against the installed version). Also **deleted `app/api/stripe/checkout/route.ts`** — zero references repo-wide (no caller, no doc, no test), sold pro/growth/enterprise/agency tiers deleted from /pricing in #243, docblock still falsely claimed "no orphaned tiers"; it was a second pricing grid surviving in the API surface. `/api/stripe/portal` KEPT (manages existing subs, 2 real callers). Rewrote both `/api/health` payment hints, which still described the old behavior. +8 tests / −1 obsolete → **1538 passed** (baseline 1531) · new tests **proven to fail against the old gate** (2 failed / 23 skipped) so they discriminate · tsc 0 · lint 0 errors (40 pre-existing) · build 0 (3 stripe routes, `checkout` gone) · proxy 61/61 · bench p99 0.481ms · structure guard PASS. New guard `single-offer-api-surface.test.ts` fails if the route returns or any route sells `mode: 'subscription'`. Doc: `docs/WEBHOOK-KEYLESS-ORDER-RECORDING.md`. → **next: founder — set `STRIPE_WEBHOOK_SECRET` in Vercel Production (Stripe → Developers → Webhooks → endpoint `https://houndshield.com/api/stripe/webhook` → copy `whsec_`) → redeploy; that ALONE turns on order recording + sale alerts. And check Stripe → Payments now: the link has been sellable since 2026-07-17 and any sale since then went unrecorded.**

- [ ] 2026-07-23 (houndshield): **shipped the 4 "everything sucks" fixes + the login-page redesign (founder boil-the-ocean).** Diagnosed first (branch == origin/main, so nothing was a stale deploy): (1) **PDF logo** — `drawBadge` drew a vector "HS" shield, real logo unused; now embeds the doberman mark as a compiled-in base64 PNG (`lib/reports/logo-data.ts` via `scripts/gen-logo-data.py`, white-chip on the dark band, `addImage` feature-detected w/ vector fallback) → real logo on every report; (2) **login/signup** — matched founder's reference: shared `components/auth/AuthTabs.tsx` Sign in/Sign up segmented toggle (preserves `?redirect=`), "Work email" labels, ToS line, real Logo, ALL working auth kept (password/OTP/2FA/OAuth/forgot); (3) **password reset** — code was already correct; the loop was that the enumeration-safe route returns a silent 200 so config failures were invisible → added value-free `reset_*` diagnostics to `/api/health` (`lib/auth/reset-diagnostics.ts`: service_role/resend/app_url(kaelus/localhost/preview flag)/sender_domain) + honest "didn't get it?" line; (4) **dashboard** — founder chose "strip it way down": signed-in operators default-hide simulated telemetry (`SIGNED_IN_STRIPPED_HIDDEN`), leaving real actions (Brain + activation checklist→PDF); public demo unchanged. +23 tests (1520/1520) · tsc 0 · lint 0 errors (40 pre-existing Next-16 warnings, none new) · build 0 · login/signup browser-verified 0 console errors · sample PDF renders the logo (257 KB). Doc: `docs/FIX-BATCH-2026-07-23.md`. → **next: founder — merge the PR (auto-deploys to prod), then `curl https://www.houndshield.com/api/health` and fix whatever `reset_*` shows (most likely set `NEXT_PUBLIC_APP_URL=https://houndshield.com` + confirm `SUPABASE_SERVICE_ROLE_KEY`/`RESEND_API_KEY`); test reset from a real Gmail. Stripe `malformed_key` remains the #1 revenue blocker.**

- [ ] 2026-07-22 (houndshield): **wired the dead RPO/MSP application funnel → live (channel #1, 0/1).** Found the "capability with no caller" dangling thread (lessons 2026-07-04): `/api/partners/apply` + `partner_applications` (mig 005) + founder-alert + branded `partner-welcome` email all existed and were tested, but EVERY `/partners` + `/partners/kit` CTA pointed at the generic `/contact` form → no RPO ever hit `partner_applications`, no founder alert, no welcome email. Built `app/partners/apply/PartnerApplyForm.tsx` (honest fetch → success only on `res.ok && data.success`, degrades to `contact@houndshield.com` on failure, never fake-success per lessons 2026-07-12; payload maps 1:1 to the route's zod schema incl. numeric `clientCount`) + SEO'd `app/partners/apply/page.tsx` (canonical/OG, sitemap, offer recap, what-happens-next). Repointed all 3 partner CTAs `/contact`→`/partners/apply`. C3PAO exclusion copy preserved. +18 tests (`PartnerApplyForm.test.tsx` behavior + `apply-funnel-contract.test.ts` regression guard: route has a caller, no CTA falls back to `/contact`). tsc 0 · lint 0 (new files) · **full suite 1497/1497** · build 0 (`/partners/apply` prerenders ○). Shipped as **draft PR #241** (Vercel preview deployed). Also fixed a **pre-existing red CI on main** surfaced by the PR: Proxy Build & Test was failing because dependabot's `better-sqlite3` 12→13 (#232) needs Node ≥22 but the proxy CI job pinned Node 20 → vitest workers crashed; bumped the job to Node 22 + corrected proxy `engines.node` to `>=22` (proxy tsc 0 · 61/61 · bench p99 1.049ms · build 0 on Node 22). Doc: `docs/PARTNER-APPLICATION-FUNNEL.md`. → **next: founder — merge PR #241 (auto-deploys to prod); then an RPO application lands in `partner_applications` (status pending) — review + set status as partners onboard. Stripe `malformed_key` remains the #1 revenue blocker.**
- [ ] 2026-07-22 (houndshield): **after-login /console redesign — real-state-first + React-wired controls (founder-directed: "buttons work + have meaning, not generic; redesign it").** Audit found buttons DID fire; the real problems were (1) the Brain-tab chips/Send/Enter were wired imperatively (`addEventListener`/`querySelectorAll('.chips button')` over React nodes — the "nothing works" smell) and (2) a signed-in operator still landed on the simulated "Acme Defense" demo with their name on top. Fixes: (1) **root-caused** the Brain controls to real React handlers (`BRAIN_TAB_CHIPS.map` + `onClick`/`onKeyDown` → `askRef.current`), deleted the imperative wiring + teardown, input stays uncontrolled so the XSS guard still holds; (2) new **`ActivationStrip`** (signed-in only) — "Connect your proxy to go live", honest state pills, real CTAs (→ Settings proxy URL / → assessment); hero pill reads **"Sample preview"** for signed-in vs "Live demo" for anon. Palette + honesty contracts deliberately UNCHANGED (aurora is founder-locked, 2026-07-18). +13 guard assertions in `LiveCommandCenter.test.tsx`. tsc 0 · lint 0 · **full suite 1479/1479** · build 0. Browser-verified on a CLEAN production build: signed-in `/console` 0 console errors, activation anchor cohesive, SPRS chip returns the operator's own honest answer. Docs: `docs/DASHBOARD-CONSOLE-REDESIGN-2026-07-22.md`. → **next: push branch → PR (Vercel preview) → on green CI, merge to main (auto-deploy) per founder's "create the pr and redeploy it". Stripe `malformed_key` remains the #1 revenue blocker.**
- [ ] 2026-07-22 (houndshield): **password reset — eliminated the founder-dashboard dependency (zero-config send, PR #231 cont.).** Built on the `/auth/confirm` route: the app now mints the recovery link **and** sends the email itself, so a working reset needs **no Supabase-dashboard changes at all**. New `app/api/auth/reset-password/route.ts` → `supabase.auth.admin.generateLink({ type:'recovery' })` (returns `hashed_token`, sends NO Supabase email) → builds `/auth/confirm?token_hash=…` → sends via the app's **Resend** integration (branded `sendPasswordResetEmail`). New pure `lib/auth/recovery-link.ts` (`recoveryRequestSchema` + `buildRecoveryConfirmUrl`, 5 tests). `forgot-password` Supabase path now POSTs this route instead of client `resetPasswordForEmail`. **Immune to A–C:** link is same-origin (`/auth/confirm` app route, no GoTrue allowlist), email is Resend-branded (no Supabase template/SMTP). **Enumeration-safe** (always `200 {ok:true}`; non-existent errors swallowed) + **no timing oracle** (Resend send moved to `after()`, off the response path — regression-guarded in `route.test.ts`) + **rate-limited** 5/min via middleware. tsc 0 · lint 0 · **full suite 1471/1471** · build 0 · both `/api/auth/reset-password` + `/auth/confirm` registered. Docs: new "zero-dashboard-dependency" section in `docs/auth-password-reset.md`. Needs only `SUPABASE_SERVICE_ROLE_KEY` + `RESEND_API_KEY` (both already in Vercel). → **next (founder): merge PR #231 — reset then works with ZERO dashboard steps. (Step A allowlist still needed for OAuth only.) Stripe `malformed_key` remains the #1 revenue blocker.**
- [ ] 2026-07-22 (houndshield): **password reset — shipped the allowlist-immune fix (PR #231).** Confirmed root cause is dashboard-only (verified #224/`baa7787` IS on `origin/main` + deployed, code chain correct) → "homepage" = GoTrue Site-URL fallback when `redirect_to` isn't allowlisted; "sent by Supabase" = default template + SMTP (app code can't set either). Added the SSR-canonical `token_hash` route `app/auth/confirm/route.ts` (`verifyOtp` → server session → recovery redirects to `/reset-password`; throws degrade to graceful "Link expired", never a 500) + pure `lib/auth/confirm-redirect.ts` (open-redirect-guarded, 6 tests). Branded recovery email points at `/auth/confirm` = **immune to the allowlist fallback** (Site URL always trusted). `/auth/callback` + `/reset-password` untouched. Full runbook in `docs/auth-password-reset.md` (allowlist + branded HoundShield template HTML + Resend SMTP). Suite **1457** · tsc 0 · lint 0 · build 0 · structure guard 0 · route redirects HTTP-verified via `next start`. → **next (founder, ~5 min, Supabase dashboard): (A) URL Configuration → add `https://houndshield.com/**` + `https://www.houndshield.com/**` (+ `localhost:3000/**`), set Site URL; (B) Auth → Email Templates → Reset Password → paste the branded template from the doc; (C) enable Resend custom SMTP for a `@houndshield.com` sender. Then click-through a real reset. Stripe `malformed_key` remains the #1 revenue blocker.**
- [x] 2026-07-21 (houndshield): **applied migration 027 to prod + hardened signup errors.** (1) Founder authorized applying **027** via Supabase MCP — cleared 3 orphan `profiles` (the founder mailbox, `info@houndshield.com`, and an internal demo Gmail account) that had no `auth.users` parent and were 500'ing every signup/invite on `profiles_email_unique` ("Database error creating new user"); added FK `profiles_id_fkey → auth.users(id) ON DELETE CASCADE`. Pre-flight proved blast radius safe (0 rows in the 4 NO-ACTION child tables, 0 report_orders, only 2 dead internal subs cascaded). Post: 0 orphans, emails freed. (2) Root-caused the `/signup` banner rendering a literal **`{}`** — the interpreter passed a Supabase `AuthError` message straight through, and a stringified Error (non-enumerable fields) is `"{}"`. New safe mapper in `lib/auth/signup-result.ts` (`extractMessage` sanitizes junk → `signUpErrorMessage` buckets duplicate-email/rate-limit/opaque-500/usable) funnels **both** Supabase AND Better Auth errors to human copy; signup page BA branch simplified to it. +9 tests (**1451** total, incl. the `{}` + Error-instance + duplicate-key regressions) · tsc 0 · lint 0 · build 0 · `/signup` browser-verified (renders, 0 console errors). Shipped in PR #229. → **next: founder — set Supabase Redirect-URL allowlist (unblocks magic-link + reset) and re-paste the `sk_live_` Stripe secret (the #1 revenue blocker, `/api/health` still `malformed_key`).**
- [ ] 2026-07-21 (houndshield): **removed the homepage hero design switcher** (founder: "just Aurora only, nothing else"). Stripped the carousel state/effects + `hd-switch` JSX + switcher CSS from `HeroDemoDashboard.tsx`; pinned the hero to `DESIGN_THEMES[0]` (Aurora). Shared `design-themes` registry KEPT — the `/console` theme picker is untouched (separate surface). Rewrote the `design-studio-contract` hero block → now asserts Aurora-only / no-switcher. tsc 0 · lint 0 · **full suite 1442/1442** · build 0 · browser-verified on prod build (Aurora renders, no switcher, 0 console errors). Not committed. → **next: founder — want the `/console` design picker gone too (Aurora-only there as well)? That's a separate, bigger change (more guard tests).**
- [ ] 2026-07-21 (houndshield): **built 3-way /login** — password (existing) + **email OTP code** + **magic link** (Supabase `signInWithOtp`/`verifyOtp`, `shouldCreateUser:false` = login-only + enumeration-safe). New `lib/auth/passwordless-state.ts` (+5 tests, enumeration-safe error mapper), `app/login/PasswordlessSignIn.tsx` (co-located, reuses the 2FA OTP helpers), wired into `login/page.tsx` via an "Email me a code or magic link instead" toggle (Supabase-mode only). tsc 0 · lint 0 · build EXIT 0 · new test 5/5. NOT browser-E2E'd (needs live Supabase config) · not committed · branch is `ponytail-install`, move to own branch before PR. → **To make each option actually fire (founder, Supabase dashboard): (1) Redirect-URL allowlist `https://houndshield.com/**` + `www` → unblocks magic link AND the existing password reset; (2) Auth → Email Templates → Magic Link → add `{{ .Token }}` → the 6-digit code shows in the email; (3) enable Custom SMTP (Resend) → branded sender + lifts the ~3–4/hr default send cap.** Stripe `malformed_key` still the #1 revenue blocker.
- [ ] 2026-07-21 (houndshield): password-reset "link goes to homepage" = Supabase **Redirect-URL allowlist never set** (the 2026-07-18 founder action still open); app code is already correct (PR #224 merged). Landing on `/` not `/login?error` proves Supabase did the Site-URL fallback = allowlist miss, not a bug. Unbranded "from Supabase" email = Supabase default SMTP (its own auth mailer, unaffected by the app's Resend). Corrected `docs/auth-password-reset.md` (was wrongly "no config required"). → **next (founder, ~1 min): Supabase → Authentication → URL Configuration → Redirect URLs → add `https://houndshield.com/**` + `https://www.houndshield.com/**` (+ `http://localhost:3000/**`) → set Site URL to the canonical host → Save → request a fresh reset link.** Branding later = enable Custom SMTP (Resend) in Supabase Auth. Separate/unchanged: orphaned-profile re-signup lockout (migration 027) + Stripe `malformed_key` (the real #1 revenue blocker).

### 2026-07-20 — Email 2FA: second auth factor (this session, cont.)
- [x] **Email 2FA shipped** (this branch → PR #227): opt-in email one-time codes on sign-in via Better Auth's official `twoFactor` plugin (email-OTP factor only, verified against installed v1.6.23 source). `/login` swaps in place to a 6-digit code step (trust-device 30d, resend w/ 30s cooldown); new `/console/security` enable/disable wizard — password → emailed code → backup codes shown once; the account flag flips ONLY after a code round-trips (`skipVerificationOnEnable:false`) so a broken email path can't lock anyone out. Migration `026_two_factor.sql` (camelCase + RLS-locked like 024, dormant until Better Auth cutover); code never in the email subject; codes 5-min expiry; 10 fails → ~15-min lockout. Pure tested core `lib/auth/two-factor-state.ts` (+12 tests) + 4 email tests → **1438/1438** · tsc 0 · lint clean · build 251/251. Maps NIST 800-171 3.5.3 + HIPAA 164.312(d). Docs: `docs/auth-email-2fa.md`. → **next: founder merges #227; on Better Auth cutover apply 026 + verify flow per doc. Founder batch COMPLETE (FAQ redesign deliberately deprioritized as subjective polish).**

### 2026-07-18 — Site-wide mobile pass (this session, cont.)
- [x] **Mobile overflow audit + fixes** (this branch → PR): drove the real production build in headless Chromium at 390×844 + 360×800 across 31 routes; found exactly 3 root causes of "site looks broken on mobile": (1) `/docs` — grid items default `min-width:auto`, so the 1fr track inherited the longest `<pre>` code line (~1028px) → whole page 679px overflow; fixed with `.docs-wrap > * { min-width: 0 }` + static (non-sticky) sidebar when stacked. (2) `/agents` — `.orb` decorations are absolute but the wrapper wasn't `relative`, so `overflow-x-hidden` clipped nothing (200px bleed); one-word fix mirroring `/about`. (3) `/status` — non-wrapping `justify-between` rows + long live health strings (150px overflow); `flex-wrap` + `min-w-0 max-w-full` on the value span. Re-audit: **CLEAN — zero overflow, 31 routes × 2 viewports**. tsc 0 · lint clean · 1422/1422 · build 251/251. → **next: email 2FA.**

### 2026-07-18 — 2nd dashboard: Compliance ROI console (this session, cont.)
- [x] **Compliance ROI dashboard** (`/roi`, PR #226 → draft): the 2nd founder-reference ("AI Cash Flow") rebuilt in dark-steel — gradient stat tiles, per-category savings bars, pure-SVG cumulative-savings trend line, mobile bottom tab bar. Live 90d/6m/12m period toggle re-resolves a tested pure model (`lib/dashboard/roi-model.ts`, 20 tests). Clearly a **modeled sample estimate** with transparent per-category remediation-cost assumptions on the page (no fabricated live telemetry). Funnels to the $499 report; the ROI number is the RPO/MSP + buyer justification. tsc 0 · lint clean · 1422/1422 · build 251/251. → **next action: after #226 merges, reset from main → site-wide mobile pass, then email 2FA.**

### 2026-07-18 — Auth reset fix + real API docs (this session, cont.)
- [x] **Password reset fixed** (PR #224 merged → live): Supabase reset links landed on `/auth/callback?redirect=/console` (logged in, skipped the password step) and `/reset-password` was Better-Auth-only. Now lands on the set-password page and supports both providers via a unit-tested `resetView` helper. Docs: `docs/auth-password-reset.md` (+ GitHub-OAuth enable steps + "name before login" explainer).
- [x] **/docs rebuilt into a real API reference** (this branch → PR): the old page pointed EVERY sidebar link at one `#sdk` anchor and shipped one JS snippet ("buttons don't work / nobody can use the API"). Now: working per-endpoint anchors, tabbed cURL/JS/Python samples with copy buttons, Authentication + "what gets detected" sections — documenting the REAL proxy endpoints (`/v1/chat/completions`, `/health`, `/v1/stats`, `/v1/events`, `/v1/quarantine`(+review), `/v1/policy`). Data-driven (`app/docs/api-data.ts`) + 9 integrity tests (no dead links, all 3 langs).
- [ ] **Founder actions:** GitHub OAuth App + enable in Supabase (steps in `docs/auth-password-reset.md`); verify "name before login" in incognito.

### 2026-07-18 — Control Map MSP dashboard + premium PDF redesign (this session)
- [x] **Control Map** (`/control-map`, PR #221 merged → live): multi-client CMMC command surface for RPO/MSP partners (channel #1), recreating the founder's reference design in the dark-steel system. Live "All Clients" selector re-scopes every KPI/framework/control via a deterministic per-client projection; all headline numbers (72% · 28 · 36 · 4) are real aggregations locked by tests; 14 controls mapped to accurate CMMC L2 → NIST 800-171 Rev 2 IDs. SSR-safe (pure SVG), mobile-first. Docs: `docs/control-map.md`.
- [x] **Fixed red CI on main** (PR #222 merged): the merged dashboard test used `getByText('72%')` but 72% renders twice (ring + framework rollup) → multiple-match throw. Switched to `getAllByText`. Root cause logged in lessons.
- [x] **Premium PDF redesign** (this branch → new PR): new shared brand layer `lib/reports/pdf-brand.ts` (steel lockup + shield mark, site URL + confidentiality footer on every page, exec summary, report id). SSP/POA&M/Evidence (`artifact-pdfs.ts`) and the audit report (`pdf-generator.ts`) now read like a real company letterhead, not bare generic-blue jsPDF. +8 brand-layer tests; all 1386 guards green; tsc/lint/build clean.
- [ ] **Next (founder batch, not yet done):** site-wide mobile pass · FAQ redesign (Vanta/Base44 pattern) · Docs + API pages (make buttons work, real usage) · 2nd dashboard (AI-Cash-Flow reference) · Brain-AI name-before-login · login-first UX + password-reset redirect + GitHub OAuth + email 2FA. Founder actions required: confirm prod delete of the founder mailbox auth row (founder full-access already in code); Supabase Auth redirect-URL allowlist; GitHub OAuth app creds. → **next action: pick the highest-revenue item (PDF is shipped; auth-login-first unblocks demos).**

### 2026-07-18 — Design Studio: live multi-design carousel + customizable console, free for everyone (this PR)
- [x] **Founder-directed follow-up to Aurora.** Pointed at the ghost cards behind the hero window and asked for a live "slider" that cycles the dashboard through several designs (light → dark → light, charts still moving), the SAME flexibility after login, and — explicitly — for it to be **free for everyone, not gated by the subscription**. Grounded the sections in real compliance-dashboard patterns via TinyFish (Vanta: framework-progress, continuous-monitoring, alerts, roadmap) so nothing is a gimmick.
- [x] **One extensible registry, both surfaces** (`lib/dashboard/design-themes.ts`): 6 designs (Aurora default · Midnight dark · Editorial · Sunset · Ocean · Forest), each with explicit CSS-var maps for the hero (`--a-*`) AND the console (`--bg/--panel/…`) + a `viz` block for the JS-painted canvas/donut. Adding one later = a single array entry → appears in the hero carousel and the console picker automatically. Aurora == the exact launch skin, so the default render is unchanged.
- [x] **Hero = live carousel** (`HeroDemoDashboard.tsx`): auto-advances every 4.6s, pauses on cursor-dwell, OFF under reduced-motion; ‹ › nav + dot rail; SVG marks retint via `var(--a-*)`; one-shot swap animation. Browser-verified Aurora → Midnight (full dark, charts/donut/bars recolored).
- [x] **Console = themeable + customizable, ungated** (`LiveCommandCenter.tsx` + `lccStyles.ts` + `use-dashboard-prefs.ts`): Appearance menu over the whole registry (retints the shell AND repaints canvas/donut/legend via `themeRef`); top bar/spine/brain-card var-ized so dark mode is genuinely correct; Customize mode reorders/hides the 7 Overview sections via CSS `order`+visibility (source JSX order untouched → all structure contracts still pass). Both prefs persist to localStorage with NO entitlement check. Browser-verified: 6 themes, real dark mode, reorder + hide, persisted across reload, 0 console errors.
- [x] **PDF export verified** (the "make sure the download buttons work, I want to test it" ask): Reports SSP/POA&M/Evidence + the public `/demo` snapshot all emit real jsPDF bytes → blob → download. Proven by unit tests (`%PDF-`, ≥4KB, ≥2 pages), the wiring component test, AND a live browser capture of the `/demo` snapshot download (valid 61 KB PDF). Console Reports stay Growth+/founder-gated by design; `/demo` is the no-account test path.
- [x] Guards +24 (**1347** total): design-themes registry contract, use-dashboard-prefs hook suite, design-studio wiring contract; aurora contract updated (donut now theme-driven, default still the pastel set). tsc + `next lint` + build all green. Doc: `docs/DASHBOARD-DESIGN-STUDIO.md`.
- [ ] ⚠️ **Honest gate (unchanged):** this is UX/personalization polish on the demo + console — it does not move revenue by itself, though a flexible, credible dashboard helps demos/close. The Guide/Plan/Assessment tabs (Tailwind `.cc-light` panels) stay light under a dark theme — a scoped cosmetic follow-up. `/api/health` money-path blockers are unchanged (Stripe key + webhook secret).

### 2026-07-18 — Aurora redesign: hero window + after-login console share one soft, clean skin (this PR)
- [x] **Founder-directed visual redesign** — pointed at a reference dashboard he loved (soft slate-blue→pale-sage gradient, glass panels, pastel lime/peach/periwinkle accents) and asked for BOTH the marketing hero window AND the `/console` after-login dashboard to wear it, so "before signup" and "after login" read as one product.
- [x] **One source of truth:** the Aurora token set (`--hs-aurora-bg`, glass, pastel accents, `--hs-action` CTA blue, `--hs-delta` green) lives ONCE in `app/globals.css :root`; the hero (`hermes.css`) and console (`lccStyles.ts`) both reference it → the two surfaces can't drift.
- [x] **Hero window rebuilt** (`HeroDemoDashboard.tsx`): retired the dark demo palette for the light Aurora window — glass ghost cards behind a floating light product window, blue brand badge, "Live AI Monitor" + blue "New scan", 4 KPI tiles with circular pastel icon badges + green deltas, pastel line/donut/bars/gauge/feed. Still a pure timer-driven demo, badged "Live demo".
- [x] **Console reskinned** (`lccStyles.ts` + `LiveCommandCenter.tsx`): shell on the same fixed gradient, glass sidebar, panels/KPIs/cards on the softer shared shadow, detection donut on the same pastel sweep as the hero, engine bars periwinkle→lime, SPRS ring lime.
- [x] **Accessibility line held:** the console's functional `OverviewCharts` bar/area marks keep their dataviz-validated categorical palette (pure pastels would drop non-text contrast below WCAG 3:1 on the real dashboard). Cohesion carried by the stage/glass/cards/donut/ring, not by making working marks low-contrast.
- [x] Guardrail: `aurora-redesign-contract.test.ts` (+10) pins tokens-once, hero-on-gradient, hero-window-not-dark, console-shares-gradient+pastel. All prior contract-locked structure preserved (spine, KPI provenance dialogs, CUI warning, captions, logo-hover tilt). tsc + lint + **1323 tests** + build green. Both surfaces browser-screenshotted and confirmed against the reference. Doc: `docs/DASHBOARD-REDESIGN-AURORA.md`.
- [ ] ⚠️ **Honest gate (unchanged):** this is a visual redesign — it does not move revenue by itself, though a soothing, credible dashboard helps demos/close. `/api/health` still reads `payments: malformed_key` + `payments_webhook: missing_secret`; the #1 blocker remains the founder re-pasting the real `sk_live_` secret key + webhook secret (`docs/GO-LIVE-STRIPE.md`).

### 2026-07-18 — Instant AI Risk Snapshot: self-serve /demo climax → warm counts-only lead (this PR)
- [x] **The demo finally ends on the PDF for every visitor.** New `/demo#snapshot` (`components/InstantSnapshot.tsx`): paste a prompt → HoundShield's **real** local engines (`BUILTIN + CMMC + HIPAA` regex patterns) scan it **in the browser** → on-screen severity + NIST 800-171 controls + estimated SPRS → **generate a branded preview gap-report PDF** → `$499` CTA + opt-in "email me + a human review". The pasted text never leaves the device — a live demonstration of the local-only moat, not just a claim.
- [x] **Privacy boundary, two enforced layers:** (1) findings on screen + in the PDF carry the pattern NAME + classification only, never the matched substring (`blockEventFromFinding` copies name/category/risk/action and nothing else) — browser-verified no raw SSN/AKIA/CAGE leaks; (2) `POST /api/report/snapshot-lead` takes **COUNTS ONLY** via a `.strict()` zod schema with no field for prompt text (smuggling attempt = 400). Cloud scanners (`risk-engine`/`gemini-scanner`) are never imported into the client path.
- [x] **Honesty per #205 doctrine:** `ReportData.snapshot:true` makes `buildComplianceDoc()` strip every claim a preview can't back — Merkle/SHA-256 audit chain → "no cryptographic audit chain", red **PREVIEW SNAPSHOT** band, SPRS score → "estimated exposure", "never transmitted to any server" attestation, "not tamper-evident" footer. On-screen copy: *"a preview, not the tamper-evident 14-day signed report an assessor accepts."* The signed sample report keeps full attestation.
- [x] Perf: `jsPDF` (~130 kB) lazy-loaded via dynamic `import()` only on generate → `/demo` First Load `190 kB` (was `332 kB`). Scan timed with `performance.now()`, surfaced as "Local scan Xms" (~8 ms live — proves <10 ms). Funnel loop: `/assessment` hero now cross-links "Prove it on your own prompt → free instant snapshot"; `docs/OUTREACH-PACK.md` send-process includes the self-serve link.
- [x] Tests +44 (**1313** total): category-nist-map, snapshot-from-scan, pdf-generator (signed-only vs snapshot-only claim sets), InstantSnapshot component (local-only copy, no raw substrings, generate→downloader `snapshot:true`, lead counts-only), snapshot-lead route (strict schema, 503+fallback, counts-only emails, `FOUNDER_EMAIL` routing). tsc + `next lint` + build green; structure guard PASS. Browser-verified live end-to-end (scan → findings → PDF on-device → CTA → lead form), 0 console errors. Doc: `docs/INSTANT-SNAPSHOT.md`.
- [ ] ⚠️ **Honest gate (unchanged):** this is top-of-funnel selling, not a taken payment. `/api/health` still reads `payments: malformed_key` + `payments_webhook: missing_secret`; the company has never taken a payment. The #1 blocker remains the founder re-pasting the real `sk_live_` secret key + setting the webhook secret (`docs/GO-LIVE-STRIPE.md`). The snapshot generates warm leads — closing them still needs working checkout or a manual invoice.

### 2026-07-17e — FAQ system leveled up: deep-linkable answers + a searchable /faq hub (this PR)
- [x] **Design research** (TinyFish web search + Stripe MCP): 2026 best-in-class FAQ patterns = deep-linkable/shareable answers, categorized search hub, actionable "learn more" links, accordion + "still need help". Verified the $499 report is live in Stripe (`price_1Tge3aQK7cyCnCHkfIfVDAGt` = `unit_amount 49900`) so pricing copy stays accurate.
- [x] `FaqAccordion` redesign (backward-compatible): every answer is deep-linkable (`/pricing#faq-<slug>` via `faqSlug()`), opens + scrolls on hash load/change, carries a one-click **Copy link** (share the exact answer), renders optional actionable `links[]` chips, `motion-reduce` transitions, a11y preserved. Scoped scroll to the component `ref` + retry — robust to React's hidden streaming buffer (`<div id="S:…">`) which duplicates ids on server-streamed pages.
- [x] New **`/faq` consolidated hub** (`app/faq/page.tsx` + `components/seo/FaqHub.tsx`): search across all 38 answers, sticky category jump-nav, deep links, empty state, the $499 `ReportOfferCard` band. **Emits NO FAQPage JSON-LD** — each Q&A already carries it on its origin page; a second copy = cross-URL duplication (advisor catch, guard-locked).
- [x] `FaqItem` gained optional `links[]`/hub `faqGroups`; sharpened 9 high-intent answers with internal cross-links (no new questions, no reworded prose — AEO word-band + cross-set uniqueness preserved). Wired `/faq` into nav (Docs dropdown), footer (Company), sitemap.
- [x] Tests +42 (1269 total): `faq-hub-contract` (coverage, unique slugs, **no FAQPage schema on /faq**), `FaqHub` component (search/empty/clear/open-reset/chips), `faqs` (links internal + no self-link, `faqSlug` unique/url-safe), extended site-chrome footer set. tsc + `next lint` + build green; `/faq` prerenders static. Browser-verified on `next start` (desktop+mobile): hub, search filter, deep-link open, chips, copy-link, 0 console errors. Doc: `docs/FAQ-SYSTEM.md`.
- [ ] ⚠️ **Honest gate (unchanged):** this is AEO/UX polish on an existing surface — it does **not** move revenue. `/api/health` still reads `payments: malformed_key` + `payments_webhook: missing_secret`; the company has never taken a payment. The #1 blocker remains the founder re-pasting the real `sk_live_` secret key + setting the webhook secret (`docs/GO-LIVE-STRIPE.md`).

### 2026-07-17d — Checkout can no longer be taken down by a bad key paste: payment-link fallback rail (this PR)
- [x] Ground truth re-checked at the source: the "expired" backup payment link (07-12 note) is **active again** — live API read shows `plink_1Tge3jQK7cyCnCHkqWpUnmze` active, selling `price_1Tge3aQK7cyCnCHkfIfVDAGt` ($499 one-time). Also confirmed: with the current `pk_` paste in `STRIPE_SECRET_KEY`, buyers were getting a raw **500** ("Failed to create checkout session") — not even the 503→/contact deflection, because the sanitized key is truthy and the Stripe call just dies.
- [x] `/api/stripe/report-checkout` now fails **toward the sale**: missing key, unusable paste (`pk_`/`whsec_` — skips the doomed API call), or a failing Stripe call all return the Stripe-hosted Payment Link for retail buyers (200 + `rail: 'payment_link'`), which no Vercel env var can take down. Verified wholesale ($299) is never downgraded to the $499 link — stays an honest 503/500. Button code unchanged (it already redirects to whatever `url` returns); /contact remains the final net.
- [x] Vertical attribution survives the static link: fallback URLs carry `?client_reference_id=report-<vertical>`; the webhook now recovers the vertical from `client_reference_id` when metadata is absent (payment-link sales carry none). Single-sourced `REPORT_VERTICALS` in `lib/stripe/report-payment-link.ts` (route + webhook both import it). `STRIPE_REPORT_PAYMENT_LINK` env override for link rotation — validated shape, degrades to the known-good link, never a broken redirect.
- [x] Tests: +14 (module round-trip/override-rejection suite, route fallback contract incl. both no-downgrade guards, webhook payment-link-sale attribution). Docs: GO-LIVE-STRIPE.md + OUTREACH-PACK.md now carry the direct buy URL sellers can email.
- [ ] 🔴 **FOUNDER — unchanged, still required:** Step 1 (re-paste the real `sk_live_` key — restores promo codes, thank-you flow, wholesale) and Step 2 (webhook secret — **until it lands, sales on EITHER rail are charged but never recorded and no sale alert is sent**; watch the Stripe dashboard for payments meanwhile). `docs/GO-LIVE-STRIPE.md` top to bottom.
- [ ] **FOUNDER:** the outreach emails can now include a working buy link even while the site key is broken — send the 10 healthcare emails (`docs/OUTREACH-PACK.md`).

### 2026-07-17b — Money-path ground truth from the live APIs + paste-proof key diagnostics (this PR)
- [x] **Ground truth, checked at the source (Stripe/Vercel/Supabase MCP access is now available in remote sessions):** the Stripe account (Houndshield.com, live mode) has **ZERO charges ever** — no customer has ever managed to pay. The $499 report price IS live and active (`price_1Tge3aQK7cyCnCHkfIfVDAGt` on `prod_Ug034JhG2q2AA7`). `report_orders` in prod Supabase: 0 rows. Vercel prod deploy pipeline healthy (latest deployment READY, 2026-07-17). Live health: `payments: malformed_key` (107-char `pk_` value) + `payments_webhook: missing_secret` — the two env vars are the entire gap between the company and revenue.
- [x] Health diagnostics now name the EXACT paste mistake instead of a generic shape error: `pk_` in `STRIPE_SECRET_KEY` → "this is your PUBLISHABLE key; the Secret key is behind the Reveal button"; `whsec_` in the key slot → swap callout; `sk_test_` → connected-but-LOUD "real cards will be declined" warning; API key pasted into `STRIPE_WEBHOOK_SECRET` → named as the wrong kind of secret. All value-free (prefix/length only), each branch unit-tested including no-leak assertions.
- [x] Deleted dead `app/api/stripe/prices/route.ts` (zero consumers, 8 hardcoded test-mode price IDs that fail live lookup — the 07-14e tech-debt item).
- [x] `docs/GO-LIVE-STRIPE.md` re-led with the 2026-07-17 reality (regression + pk-vs-sk Reveal walkthrough + "check the first 8 characters before saving"); stale "STEP 1 IS DONE" banner replaced.
- [x] Outreach pack REBUILT into the repo as `docs/OUTREACH-PACK.md` (the 07-12c original lived in a session scratchpad and was lost; the runbook pointed at the dead path). Direct cold emails for Rachel/Marcus/Jordan + follow-up/breakup + subject bank + $499 blurb + send process, doctrine-clean.
- [ ] 🔴 **FOUNDER — the same 5-minute fix, now with the exact key named:** Stripe → Developers → API keys → **Secret key → Reveal** → copy → Vercel `compliance-firewall-agent` → `STRIPE_SECRET_KEY` (Production ticked) — **verify it starts `sk_live_` before saving** → redeploy → health reads `payments: connected`. Then Step 2: webhook endpoint + `STRIPE_WEBHOOK_SECRET` (GO-LIVE-STRIPE.md). Zero charges ever on the account — every day this stays broken is a day the company cannot take money.
- [ ] **FOUNDER:** send 10 healthcare emails from `docs/OUTREACH-PACK.md` (the process section takes ~45 min). A yes closes via manual invoice even while Stripe is down.

### 2026-07-17c — PR #207 closed as superseded; its one good idea salvaged (this PR)
- [x] Founder asked "what's wrong with #207?" — answer: nothing in content, it was the LOSING twin of two competing repairs of the 07-16 dependabot breakage (#209 merged first; every fix in #207 verified already on main), left open and now conflicting (`dirty`; merging would have REVERTED main's Stripe apiVersion to the older clover release). Closed with an explanatory comment.
- [x] Salvaged its one net-new idea: `lib/stripe/api-version.ts` — the ONE place the Stripe API version is pinned; all 5 `new Stripe(` sites now import `STRIPE_API_VERSION` instead of hardcoding `'2026-06-24.dahlia'` five times. Next stripe SDK major fails tsc in exactly one file.
- [x] Contract test (`api-version-contract.test.ts`): version-shape check + no quoted `apiVersion:` outside the canonical file + every `new Stripe(` site references the constant (≥5 sites sanity floor). Negative-tested: injected a hardcoded pin → guard failed → reverted.

### 2026-07-17 — Console data provenance: click any number → see where it comes from (MERGED #208, live in prod)
- [x] Founder: "when we click on anything it should show the data where it is fetching from, not just numbers." Shipped: every figure on /console is now clickable and opens a provenance dialog — What this is · Where it comes from (named demo seed / this device's localStorage / subscription entitlements / product fact) · How it updates · How it becomes your data (with an Open Settings · Proxy URL CTA while simulated).
- [x] Surfaces wired (19 registry entries): 4 KPI tiles (now real buttons with ⓘ), hero capability chips, status strip, evidence-chain spine ("Simulated preview" pill now opens the audit-chain provenance instead of silently jumping tabs), throughput, donut, engine bars, threat feed (header chip + any row via delegated listener), SPRS ring, all 4 overview charts, assessment snapshot + board header, Settings usage meters, Brain budget.
- [x] Honesty mechanics: `resolveProvenance(id, live)` — signed-in viewers get the account/on-device variants ("nothing is simulated for your account"); simulated entries MUST name a go-live path (unit-enforced); `SourceChip` without a handler degrades to a plain span (never a dead button).
- [x] A11y: dialog is role=dialog + aria-modal + labelled, Escape/backdrop/button close, focus moved in and restored; chips carry aria-haspopup.
- [x] Guards: +40 tests (1227 total) — registry contract (no dead dialogs, no orphan entries, no cloud-scanning copy), dialog suite, click-flow integration tests, and a "data provenance" block in console-dashboard-contract. Browser-verified on `next start`: KPI dialog, spine dialog, Escape, Settings CTA, meter chip — 0 console errors. Doc: docs/DASHBOARD-DATA-PROVENANCE.md.

### 2026-07-16 — Console made honest for real customers + a11y overhaul (PR pending)
- [ ] 🔴 **FOUNDER — payments REGRESSED `connected` → `malformed_key`.** `/api/health` now reads: key is set (107 chars) but doesn't start with `sk_` — that's the **publishable `pk_live_` key pasted into `STRIPE_SECRET_KEY`** (both are ~107 chars). Re-paste the SECRET key (Stripe → Developers → API keys → "Secret key") into Vercel Production scope → redeploy → health must read `payments: connected` again. `payments_webhook: missing_secret` also still open (GO-LIVE-STRIPE.md Step 2). Checkout was LIVE on 07-14; it is dead again until this is fixed.
- [x] Multi-agent audit of /console (a11y + data-honesty finders; 4 finders + all verifies lost to session limit — findings verified by hand instead). 17 findings confirmed, all fixed:
- [x] **Data honesty (the big one):** Brain AI answered signed-in customers with the sample org's fabricated numbers ("your SPRS is +78", "you closed 3.5.10 this week", and an invented "No spill occurred; no reportable event" incident determination) while the guide tab showed their real localStorage score. Now: `BrainContext.own` — signed-in asks compute from the operator's real assessment (lazy import, recomputed per ask); not-assessed gets an honest nudge; incident answers NEVER emit a spill determination on any path; demo answers are source-tagged `· sample data`.
- [x] Simulated-telemetry labeling: persistent "Simulated preview" pill in the evidence spine (opens Settings), "demo/sample" wording on every live-tag, feed, donut, engines, KPI deltas; OverviewCharts de-possessived + sample-tagged. Replaces the untrue "on your hardware" spine claim.
- [x] Fake-success chrome KILLED structurally: Settings "Reveal→Revealed"/"Edit→Saved" stubs + fabricated `hs_live_…`/`sk-or-…` masked keys replaced with honest not-provisioned/keyless rows; CopyRow is now Copy-only. Usage meters: real customers see 0 scans / 1 seat (never the fabricated 57%-used seeds — those are demo-only); first-run checklist no longer pre-checks steps 1-2 for real accounts.
- [x] Assessment tab: hardcoded "+78 ring / AI-ranked wins" replaced by `AssessSnapshot` — computes ring + impact-ranked fastest wins from the operator's own answers, live-updates via new `hs-assessment-updated` event dispatched by the storage layer; sample-labeled on the public demo.
- [x] Counter drift fixed at the source: KPI seeds now import `SCANS_24H`/`BLOCKED_TODAY` from OverviewCharts (SSR text == effect seeds == chart sums); quarantine seed unified (was: markup 15, counter 6 → snapped 15→7 on first event).
- [x] **Sign-out existed NOWHERE in the console** — new provider-agnostic `SignOutButton` (Better Auth flag or Supabase; honest failed-retry state, no redirect on failure) in the sidebar for signed-in operators.
- [x] A11y: aria-current on tabs; Brain input labeled + transcript is a polite live-region (typing bubble aria-hidden); closed mobile drawer leaves tab order (visibility transition — transform alone left ~11 invisible focusable controls); scrim out of tab order; burger aria-expanded; canvas chart alt; ticking clock/p50/spine decor aria-hidden; reduced-motion now silences KPI bump/feed flash/pulse dots; PDF success announced (role=status). Contrast: new `--ok/bad/warn/orange-text` AA tokens on all small status text — worst offender was the MANDATED "Do not enter CUI" warning at ~2.5:1.
- [x] Guards: +32 tests (1187 total) — honesty contracts (no spill determination, sample tags, seed single-source, no fake-success chrome, sign-out present) + a11y contracts in console-dashboard-contract; SignOutButton + AssessSnapshot suites.
- [x] Supabase MCP added to .mcp.json (project scope) + agent skills installed. **FOUNDER:** run `claude` → `/mcp` → authenticate supabase (OAuth needs an interactive terminal).

### 2026-07-15c — Mono → Geist Mono, third-party font CDN eliminated (same PR #204)
- [x] "Evolve further": found the mono face was loaded via a runtime `@import` from the Google Fonts CDN — every visitor's browser called Google (IP leak; the basis of real GDPR rulings) on a site whose footer promises zero data exfiltration, AND Tailwind's `font-mono` never referenced it, so the site rendered two different monos. Now: **Geist Mono** self-hosted via next/font (`--font-mono` on the root html), all five stack definitions unified (globals, hermes, tailwind, lccStyles, HeroDemoDashboard — the last still had a stray serif fallback), CDN `@import` + preconnects deleted.
- [x] Guard extended (font-brand-contract): fonts.ts must load Geist_Mono; six stack files may not reference old families, serif fallbacks, or ANY font CDN (googleapis/gstatic/typekit/bunny). Browser-verified: computed mono = "Geist Mono", external font requests across /, /pricing, /console = **zero**.

### 2026-07-15b — Brand typeface → Geist everywhere (founder call, same PR #204)
- [x] Founder shared a base44.app ad ("I love this font, use it everywhere"). The ad face is a commercial-class neo-grotesque; closest open-licensed (OFL) production match is **Geist**, self-hosted at build time via next/font (zero external font requests, GDPR-clean). Swapped in ONE place — `app/fonts.ts` now loads Geist into both `--font-display` and `--font-body` — so hermes pages, v3 pages, dashboard, and command center all switch together (no split-brain). Fallback stacks de-serifed in globals.css, hermes.css, tailwind.config.js (font-editorial), lccStyles.ts.
- [x] Guard: `app/__tests__/font-brand-contract.test.ts` — fonts.ts must load Geist for both roles; the four stack-definition files may not reference the old families or any serif fallback. It caught a stale comment on first run. `public/hermes-demo.html` + `_bootstrap.html` are byte-equality archives and stay untouched by design.

### 2026-07-15 — FAQ system rebuilt sitewide + $499 offer card (PR pending)
- [x] Founder: "improve the FAQ section of every page… and the $499 thing, make it a proper button." Rebuilt the shared `FaqAccordion` (card-per-question, readable type — questions were 14px, now 16–17px; answers 15px/1.7 — steel open-state, a11y ring, aria-controls) and `FaqSection` (eyebrow + display heading + "Still have questions?" contact row + coupled FAQPage JSON-LD). All 5 hand-rolled FAQ implementations (contact, answers/[slug], partners/kit, products/[industry], pricing/features inline blocks) replaced with the one shared system.
- [x] FAQs ADDED where missing: homepage (new `homeFaqs`), /how-it-works (orphaned `howItWorksFaqs` finally rendered), /assessment (new `reportFaqs`). Contact FAQ content moved to `contactFaqs` with two factual fixes (free tier = 110 controls read-only + 1,000 prompts/mo, not "25 controls"; PDF exports = Growth+, not "PDF and CSV on all plans").
- [x] pricingFaqs rewritten: now leads with the $499 one-time report, kills the "$499 report vs $499/mo Growth" collision with an explicit Q&A, and removes the banned "Federal at $2,499/month" tier claim (NEVER-DO).
- [x] $499 CTA: new `ReportOfferCard` (pitch + included checklist + trust row | big $499 panel + full-width buy button + "talk to us first" + sample-report link) — now the hero of /pricing AND a band on the homepage before the CTA band. `.section.alt` (used but never defined) now styles a real alternate band.
- [x] Guards: `app/__tests__/faq-surface-contract.test.ts` (FAQPage JSON-LD ⇒ visible FAQ UI; core pages must carry a FAQ; home+pricing must carry the offer card) + new datasets added to the AEO faqs test (uniqueness/snippet-length enforced). 1151/1151 tests, tsc, lint, build green; 20 prod-server screenshots (10 pages × desktop+mobile), 0 console errors.
- [x] Checkout note (corrected in the #204 merge): `payments: connected` went LIVE in #187/#202 (see 2026-07-14e below) — the new $499 buy buttons open real Stripe Checkout, not the /contact deflection. The remaining founder action is the webhook secret, tracked below.

### 2026-07-14e — STRIPE IS LIVE: paste-proof key handling + first live checkout verified (MERGED #187 + #202, live in prod)
- [x] Founder: "every paste comes out empty — fix it or recreate the placeholder." Shipped `lib/stripe/env.ts` (auto-cleans quotes/newlines/zero-width chars/whole-line `NAME=value` pastes; ALL six Stripe routes now read through it) + `/api/health` diagnostics: `payments: connected|malformed_key|missing_key` and now `payments_webhook: configured|malformed_secret|missing_secret`, each with a value-free `*_hint` naming the exact fix. Leak-proof by tested contract.
- [x] **`payments: connected` on prod — the 2-week #1 blocker is CLEARED.** Verified beyond the health check: drove the real buyer flow in a browser → `/pricing` → $499 CTA → **live Stripe Checkout (`cs_live_…`) showing "CMMC AI Risk Assessment Report — $499.00"**. A customer can pay today.
- [ ] **FOUNDER — new #1 (5 min): the webhook.** Prod reads `payments_webhook: missing_secret` → a card can be CHARGED but the order is never recorded and no sale alert is sent. Do `docs/GO-LIVE-STRIPE.md` Step 2: Stripe → Developers → Webhooks → endpoint `https://houndshield.com/api/stripe/webhook` → copy `whsec_…` → Vercel (project compliance-firewall-agent, Production ticked) as `STRIPE_WEBHOOK_SECRET` → redeploy → health shows `payments_webhook: configured`. Then the Step 4 test-card dry run.
- [x] Tech-debt CLOSED 2026-07-17b: dead `app/api/stripe/prices/route.ts` deleted (zero in-app consumers, 8 hardcoded price IDs failing live-mode lookup).

### 2026-07-14d — Deep-dive audit of the session's work (PR pending)
- [x] Reviewed the cumulative #183–#185 diff + click-drove every flow. Found & fixed: Settings "Copy" button was ANOTHER fake-success stub (now really writes the clipboard, only confirms on success — regression-tested); hour-axis labels clipped at the chart edges (inward anchors); duplicate React keys possible on rapid re-exports (monotonic id). Browser-verified: guide→"Begin assessment" opens the inline board in place with the hash cleared; clipboard actually contains the proxy URL.
- [x] Data polish (no deploy): founder profiles get company='HoundShield' → PDFs now read SSP_HoundShield_… and the sidebar shows the org, not the first name.

### 2026-07-14c — Dashboards made diverse + self-explanatory (MERGED #185, live in prod)
- [x] Founder: "more graphs, more colours, small explanations, consistent data — console AND the hero-page dashboard." Console Overview gains 4 dataviz-validated panels (`OverviewCharts`): Activity by hour (stacked, blocked share), Where prompts go (ChatGPT/Copilot/Claude/Other), SPRS 30-day trend vs the L2 target line, Risk mix (severity stack with counts). Every panel carries plain-English microcopy + accessible labels. Homepage hero gains a "Where prompts go" panel + captions on every chart.
- [x] Consistency is now a TEST: chart sums must equal the KPI seeds (142,690 scans / 2,233 blocked) or the build fails — the contract caught its first mismatch immediately.

### 2026-07-14b — Report PDFs made REAL + founder account live (MERGED #184, live in prod)
- [x] Founder: "generate PDF was not working." Root cause: the console Reports tab's three buttons were stubs (label flip, no file — the fake-success anti-pattern again). Built `lib/reports/artifact-pdfs.ts` (SSP / POA&M / C3PAO Evidence Pack — three DISTINCT jsPDF artifacts from the user's own local assessment) + `ReportsPanel` (real download, true SHA-256 in Recent exports, visible errors, honest empty state). Growth+ gated with the priced restriction routing to Plan & Unlocks; **founder always free**.
- [x] Founder account made real: created the founder mailbox in prod Supabase auth (pre-confirmed) + agency profile/subscription grants for it, `the founder's personal Gmail`, `info@houndshield.com` (demo account left Free). Browser-verified live sign-in → "Founder · full access", nothing locked.
- [ ] **FOUNDER:** change the temp password after first sign-in (chat-transmitted); create the the founder mailbox mailbox in Hostinger so password reset emails can ever work.

### 2026-07-14 — Console redesign: dashboard-first + founder full access (PR pending)
- [x] Founder feedback on the 07-13 console: the "buy this / do this" stack above the dashboard is gone. `/console` = the Live Command Center alone; the guide (CustomerStatusPanel) and the paywall (PlanUnlocksBoard, ex-ConsoleDashboard) moved into SIDEBAR buttons ("Your Guide", "Plan & Unlocks"). Locked tiles now price the restriction ("Available on Growth — $499/mo"). Assessment = mid-list tab, runs INLINE (lazy board, `#assessment` hash opens it); the bounce link to /command-center/shield/assessment removed.
- [x] Founder access: the founder mailbox (lib/billing/founder-access.ts, env-extendable) → top-tier entitlements with NO payment across ALL gates: console viewer, /api/me, /api/customer/status, getUserSubscription (PDF 402 + gateway). Session-email keyed, fail-closed, unit-tested.
- [x] Fixed 2 latent rendering bugs the browser pass caught: `.hs-lcc *` reset was nuking Tailwind spacing inside embedded panels (now zero-specificity `:where()`); cc-light lacked text-white/45/55/65 remaps (white-on-white text). Screenshot-verified desktop+mobile, 0 console errors. 1087/1087 tests, build+lint+tsc green.
- [ ] **FOUNDER:** after merge, sign in on houndshield.com with the founder mailbox and verify: sidebar shows "Founder · full access", Plan & Unlocks all unlocked, PDF export works without paying. (The account must EXIST via sign-up first — founder access keys on the login email.)
- [ ] **FOUNDER — still the #1 revenue blocker (unchanged):** `STRIPE_SECRET_KEY` Production scope in Vercel → `/api/health` must read `payments: connected`.

### 2026-07-13 — Tier-gated after-login console + inline assessment (PR pending)
- [x] Founder asked (looking at live site): don't bounce "Begin assessment" to a deep link — put it on the same after-login dashboard; and build a restricted dashboard for free users / everything for paid, per the plan. Ran the HERMES CHALLENGE first (this is Stage-2 subscription plumbing, gates tiers nobody can buy while `payments:missing_key`, on a console ~nobody reaches). Founder: **"you decide" + boil-the-ocean** → informed override. Chose the on-plan version: build it, but every upgrade CTA funnels to `/pricing` (which leads with the live $499 report).
- [x] Reused the existing single source of truth `lib/billing/entitlements.ts` — did NOT build a new entitlements model. New pure projection `lib/billing/console-sections.ts` → `buildConsoleSections(tier)` = `{isPaid, unlocked[], locked[]}`; locked tiles carry a truthful "Available on <tier>" (via `tierThatUnlocks`) + Upgrade→/pricing.
- [x] Extracted the 110-control board into `components/dashboard/AssessmentBoard.tsx` (one source; `embedded` prop). Route `shield/assessment/page.tsx` is now a thin wrapper. Console mounts it INLINE (lazy `next/dynamic`, opens on `#assessment`) — themed correctly because it renders inside the same `.cc-light` scope the command-center already uses. Core assessment logic untouched (advisor's blast-radius warning).
- [x] `CustomerStatusPanel` "Begin/Continue assessment" CTA now targets `#assessment` (opens the inline board) instead of the standalone route — fixes the exact bounce the founder pointed at. `lib/customer/status.ts` left canonical.
- [x] Tests: `console-sections.test.ts` (11 gating-contract) + `ConsoleDashboard.test.tsx` (5 render/gating). tsc + targeted suite green. Docs: `docs/CONSOLE-TIERS.md`.
- [ ] **FOUNDER — this ships a dashboard, it connects NO revenue.** Still the only money blockers: (1) fix `STRIPE_SECRET_KEY` Production scope → `payments: connected`; (2) send the first outreach email. The console reaches ~0 users until login + payments are live.

### 2026-07-12d — Founder gets alerted when a report sells + go-live runbook (PR pending)
- [x] Traced the full $499 money path end-to-end (button → checkout → thank-you → webhook → order row → emails). All links exist EXCEPT: on a sale the webhook emailed only the **buyer**, never the founder. The report is manually fulfilled → founder needs an actionable "go deploy for X, start their 14-day assessment" alert (Stripe's default receipt only says "you got $499"). Added `reportOrderEmail.founderAlert(...)` (HTML-escaped buyer input) + wired it into `handleReportOrder` (best-effort, never blocks billing) → `FOUNDER_EMAIL || contact@houndshield.com`. Retail/$499 vs wholesale/$299 aware.
- [x] Tests: 8 template unit tests + 3 webhook integration tests (buyer+founder both emailed, FOUNDER_EMAIL fallback, email-throw never fails the webhook). `vi.hoisted` resend mock. Local suite green.
- [x] Wrote `docs/GO-LIVE-STRIPE.md` — founder runbook to actually take money. Leads with the REAL blocker: `payments:missing_key` persisted AFTER a redeploy, so it's not a missing redeploy — it's Production-scope / name-typo / stray-quotes on `STRIPE_SECRET_KEY`. Then webhook registration, FOUNDER_EMAIL, test-card dry run, manual-invoice fallback.
- [ ] **FOUNDER:** work `docs/GO-LIVE-STRIPE.md` Step 1 first (fix the env-var scope) → `/api/health` must read `payments: connected`. Merging PRs alone will NOT fix Stripe.

### 2026-07-12c — Outreach pack produced (removes "I don't know how to sell")
- [x] Wrote ready-to-send outreach: 4 cold emails (healthcare/legal/defense direct + RPO co-sell), follow-up, subject bank, $499 offer blurb, and a step-by-step "how to send" (find contacts → 10/day → book call → invoice manually while Stripe is off). Saved: scratchpad/outreach-pack.md.
- [ ] FOUNDER: pick Healthcare or Legal (fastest, no FedRAMP blocker), send 10 today. First "yes" → manual invoice (don't wait for Stripe key).

### 2026-07-12b — Stop losing leads (houndshield session, PR pending)
- [x] Freshness re-check: CMMC Phase 2 still **Nov 10 2026** (no slip) → CONTINUE verdict holds. Idea validation NOT re-run (merged #177 12h earlier).
- [x] **Contact form was FAKE** — `handleSubmit` did `setTimeout → "Message sent"` and delivered NOTHING. Every lead (incl. $499 buyers deflected from dead Stripe checkout) was silently dropped. Fixed: real `POST /api/contact` (new route, Resend → founder inbox, mirrors `/api/partners/apply`). Honest 503 fallback (shows direct email) when Resend unset — never a fake success. Browser-verified: real POST, honest fallback shown, 0 console errors.
- [x] Removed banned false claim on /contact ("hosted on FedRAMP-authorized cloud services") — NEVER-DO violation; replaced with the honest Mode-B / non-CUI-trial wording.
- [x] /pricing: $499 one-time report is now the **hero h1** (doctrine: lead product first); subscription grid reframed "Ongoing monitoring" → kills the "$499/mo Growth vs $499 one-time" collision. Guard test (direction-a-port) green.
- [x] Guards added: `app/api/contact/__tests__/route.test.ts` + `app/contact/__tests__/contact-delivery-contract.test.ts` (fake-form + FedRAMP-claim can't silently return). Full suite 1026/1026, build+lint+tsc green.
- [ ] **FOUNDER, 5 min — STILL THE #1 BLOCKER:** set `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` in Vercel, point webhook at `https://houndshield.com/api/stripe/webhook`, redeploy, verify `/api/health` shows `payments: connected`. Until then checkout 503s (but leads now survive via the fixed /contact form).

### 2026-07-12 — Idea validation + houndshield skill upgrade (this session)
- [x] 7-axis market validation (TinyFish web research + adversarial verify). VERDICT: **CONTINUE** — product is done, market real, CMMC timing intact; the gap is checkout + distribution, not product. Full writeup: `docs/VALIDATION-2026-07-12.md`
- [x] `houndshield` skill upgraded — now self-orients (reads todo/lessons/primer/health → real briefing, not placeholders), keeps all 12 personas, logs each session for continuity
- [x] Confirmed BOTH payment paths dead: `/api/health` → payments:missing_key (checkout 503) AND backup link buy.stripe.com/aFa00lgzIgJx3Aqb7qgUM00 → **EXPIRED** (2026-07-12). Company cannot take a dollar until fixed.
- [ ] **FOUNDER, 5 min — THE #1 BLOCKER:** set `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` in Vercel, point webhook at `https://houndshield.com/api/stripe/webhook`, redeploy, verify `/api/health` shows `payments: connected`. (Backup Stripe payment link is expired — must create a fresh one OR set the key.)
- [x] DONE 2026-07-12b: /pricing now leads with the $499 one-time report (hero h1); "$499/mo Growth" vs "$499 one-time" collision fixed by reframing the grid as "Ongoing monitoring". Didn't wait for payments-green — clarity fix needs no live checkout, and the report CTA safely deflects to the now-working /contact form.
- [ ] FOUNDER: send the 10 RPO/MSP emails as a **referral/co-sell pilot** (not white-label — RPOs gate white-label on SOC 2 + insurance + references); land 1 design-partner testimonial (free/half-price $499 report for a case study).

### 2026-07-11 — Vertical-balance tranche
- [x] Legal cluster opened (Marcus had ZERO articles): /blog/ai-attorney-client-privilege-what-bar-opinions-mean
- [x] /answers/does-vanta-or-drata-cover-ai-usage — complementary-tool traffic capture, honest "we don't replace them" framing
- [x] Verified Stage-1 gates already live: Brain AI CUI warning (GlobalChat) + RFC 9116 security.txt (tested)

### 2026-07-10 — Evidence-moat tranche (PR #175, after competitive deep-dive)
- [x] Gaps page: "AI-relevant gaps / SPRS points at stake" banner (honest scope, uses the contract-tested relevance set)
- [x] Changelog 2.7.0 — real shipped features only (control library, partner kit, console links, latency CI gate)
- [x] docs/EMAIL-SEQUENCES.md — RPO outreach 3-touch (arms the 10 drafts) + $499 buyer onboarding day 1/3/7 (manual send until wired)
- [x] /answers/what-evidence-does-a-c3pao-accept-for-ai-usage — the pre-purchase Jordan question, four-artifact table
- [ ] **FOUNDER:** approve the weekly competitive-watch Routine + TinyFish MCP in an interactive session (both blocked on approval here)

### 2026-07-10 — Second tranche (founder said "go"; fresh branch off merged #159)
- [x] /controls — 110 programmatic NIST 800-171 pages + index, honest AI-relevance verdicts, contract-tested
- [x] Commercial answers (nightfall-alternatives · cmmc-ai-monitoring-cost) + C3PAO checklist article + /partners/kit (RPO/MSP)
- [x] CI: scanner latency bench (<10ms p99 gate; measured 0.565ms) · security-audit (report-only) · Dependabot · Lighthouse CI (informational)
- [ ] **FOUNDER, 5 min:** STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET in Vercel — still THE revenue blocker
- [ ] **FOUNDER, 5 min:** DOCKERHUB_USERNAME/TOKEN secrets + push tag `proxy-v2.0.0` → publishes houndshield/proxy:latest
- [ ] **FOUNDER:** send the 10 RPO/MSP emails (now with /partners/kit to land on); Y/N on the two frozen-content flags (llms.txt "C3PAO partner program", "Kaelus" in old post)

### 2026-07-10 — Tier-1 SEO content sprint (same branch/PR #159, additive-only per founder freeze)
- [x] 5 Tier-1 blog articles shipped (GCC High cost math · HIPAA Privacy Officer 2026 · CUI incident-response playbook · NIST 800-171 AI mapping · AI policy template)
- [x] 3 AEO answers (is-chatgpt-hipaa-compliant · can-law-firms-use-chatgpt · what-happens-if-you-paste-cui-into-chatgpt)
- [x] 2 comparisons (/compare/strac · /compare/witnessai) — pass the honest-editorial guard
- [x] llms.txt: insert-only additions (new articles + answers + compare list); blog SEO contract test added to CI
- [ ] **FOUNDER FLAG:** llms.txt still says "C3PAO partner program" (channel-identity framing — the barred kind per 2026-06-23 lesson) and the frozen blog post "how-to-protect-cui…" says "your Kaelus endpoint" (stale brand). Both left untouched per the don't-touch-existing-SEO freeze — approve fixes and they're one-line edits.

### 2026-07-10 — Unseen-issues sweep (branch claude/project-unseen-issues-206sz6)
- [x] Proxy test suite un-broken (root postcss.config.mjs leak) + proxy added to CI (it had ZERO CI coverage)
- [x] docker-publish.yml: publish steps could never run (step env invisible to its own `if:`) — fixed with `secrets` context; `houndshield/proxy:latest` can now actually publish once DOCKERHUB_* secrets are set
- [x] `npm run verify:structure` failing since PR #146 — manifest + PROJECT-STRUCTURE.md synced, added as CI Structure Guard job
- [x] ESLint 153→0 warnings (eslint-plugin-unused-imports + `^_` pattern); pre-commit `--max-warnings 0` no longer landmined
- [x] SecurityAdvisor system prompts were never sent to the model (JSON parse always fell back) — wired via ReasoningLoop `systemPrompt`
- [x] /auth sign-up now shows a confirmation banner (was silent after success); fake "Live Scanning" counters relabeled "Demo — Simulated Traffic"

### 2026-07-08 — Reality check (HERMES session: direct GitHub push restored)
- [ ] **FOUNDER, 5 min — THE revenue blocker:** set `STRIPE_SECRET_KEY` in Vercel.
      Live `/api/health` says `payments: missing_key` — ALL checkout (subscriptions + the $499 report) is dead until this lands.
      While there: set `STRIPE_WEBHOOK_SECRET` + point the Stripe webhook at `https://houndshield.com/api/stripe/webhook`, then re-verify `/api/health`.
      (`database` and `ai_router` show ✅ connected — those 06-10 env items are done.)
- [ ] Merge PR #151 — /pricing $499 report CTA now uses the ReportCheckoutButton rail (503-safe while the key is missing). Merge = auto prod deploy from main.
- [ ] **FOUNDER, ~20 min:** send the 10 C3PAO partner emails sitting in Gmail drafts (from 06-10 — status unknown; still the highest-leverage GTM action if unsent).
- [x] `hermes/redesign-demos` pushed (503e114 — HERMES site Direction A); Vercel preview build READY (2026-07-08)
- [x] 06-10 "push feature/beast-ui-v3" task CLOSED as obsolete — main independently shipped the CTA / testimonial / og-image fixes via PRs #103–#149; the one surviving gap (direct $499 CTA on /pricing) is PR #151 (2026-07-08)

### Superseded 2026-06-10 audit items (kept for trail)
- [~] Push `feature/beast-ui-v3` — obsolete; superseded by main (see above)
- [x] Supabase + OpenRouter env vars — verified `connected` via live /api/health (2026-07-08); Stripe keys remain the open item
- [x] Gap Report $499 payment link LIVE → https://buy.stripe.com/aFa00lgzIgJx3Aqb7qgUM00 (`prod_Ug034JhG2q2AA7`)

## Sprint 1 (Week of 2026-04-28) — Jordan deploys in 10 minutes, exports a PDF ✅ MERGED PR #62

**Sprint goal:** One customer deploys HoundShield and exports a PDF audit report for her C3PAO.

- [x] Complete `proxy/install.sh` — curl pipe bash, starts Docker, configures proxy URL + `--uninstall` + `--verify` flags
- [x] Wire PDF generation in `app/api/reports/generate/` — jsPDF, 5-page C3PAO report, tier gating, demo mode
- [x] E2E test: proxy intercepts ChatGPT traffic -> CUI flagged -> PDF exported (105/105 passing)
- [x] Landing page: Jordan pain copy ("Your engineers are using ChatGPT. Your assessor is 6 months away."), Anthropic/Claude coral palette, `/docs/quickstart` page
- [x] Push Supabase migrations to production — 010 (RLS fix + AI observability) + 011 (partner portal) applied 2026-05-13
- [ ] Set `OPENROUTER_API_KEY` in Vercel — Brain AI shows "trouble connecting" on live site (manual step)

## Sprint 2 (Week of 2026-05-05) — First C3PAO partner, first paying customer

- [ ] Contact 10 C3PAOs from marketplace.cmmcab.org (use /browser-harness to scrape the list)
- [ ] Record 3-minute demo: CUI paste -> ChatGPT block -> PDF report export
- [ ] Set `STRIPE_WEBHOOK_SECRET` in Vercel dashboard
- [ ] Build `/partner` landing page — C3PAO referral program with commission structure
- [ ] Publish one technical blog post: "Why cloud-based AI DLP violates DFARS 7012"

## Sprint 3 (Week of 2026-05-12) — Brain AI queryable

- [ ] Wire `brain-query.ts` to Brain AI API route (`app/api/brain/query/route.ts`)
- [ ] Ingest CMMC framework docs via /firecrawl-ingest skill
- [ ] Ingest competitor profiles (Nightfall, Strac, Purview) via /firecrawl-ingest
- [ ] Add `GET /api/brain/ask?q=...` endpoint for founder + agent queries
- [ ] Verify: 20 test questions return correct answers before marking complete

## Sprint 4 (Week of 2026-05-19) — 5 paying customers, $1K MRR

- [ ] Onboarding email sequence (3 emails: day 1/3/7)
- [x] In-app CMMC control coverage map — shows Jordan which controls HoundShield covers (shipped PR #77)
- [ ] SPRS improvement estimate shown in dashboard (+15 to +30 points)
- [ ] C3PAO white-label dashboard MVP (rebrandable)

## Backlog (post $10K MRR)

- [ ] Azure Sentinel connector
- [ ] Splunk HEC integration
- [ ] Blockchain-anchored audit trail
- [ ] SIEM integration suite
- [ ] Multi-tenant C3PAO portal
- [ ] HITL review workflow improvements
- [ ] Mobile app
- [ ] OWASP Top 10 full security review + automated dependency scanning
- [ ] Load testing: verify <10ms latency at 1,000 req/sec (k6 or autocannon)
- [ ] Gemini Flash as secondary scanner for speed-sensitive paths (sub-5ms deterministic)
- [ ] CI/CD GitHub Actions: lint + tsc + jest gates on every PR (see `.github/workflows/`)

## Backlog — REPO_INTEGRATION_MAP (external repos, post $10K MRR)

> See `docs/REPO_INTEGRATION_MAP.md` for full context on each repo.

- [ ] browser-use — browser automation for Jordan's proxy setup walkthrough
- [ ] firecrawl — ingest CMMC/NIST docs into Brain AI knowledge graph
- [ ] ai-website-cloner — competitive intelligence ingestion
- [ ] space-agent — multi-agent orchestration for compliance audit flows
- [ ] swarm-forge — swarm-style agent patterns for parallel compliance checks
- [ ] OpenMythos / OpenHarness — agent harness patterns for proxy pipeline
- [ ] anywhere-agents — cross-platform agent deployment (Windows/Mac/Linux support)
- [ ] little-coder — lightweight in-browser coding agent for C3PAO portal
- [ ] ruflo — workflow automation for onboarding email sequences

## Done
- [x] **Pre-launch polish sweep (2026-06-11)** — Doberman logo everywhere + favicons/OG; 21 public pages migrated to v3 light-steel (NavV3/FooterV3); pricing invisible-price fix + $/mo annual presentation; Brain AI identity FAQ (works keyless); /sign-up 404 CTA fix; /roadmap page; sitemap fixes; "Hound Shield"→"HoundShield" in 110+ files; 409/409 tests
- [x] **OODA loop — full behavioral DLP engine** (`proxy/ooda/`) — commit `984c3b36`
  - 10 source files: types, db, rate-tracker, baseline, observe, orient, decide, act, loop, server migration
  - 49 vitest tests, real SQLite temp dirs, zero mocks — all green
  - `npm run build` clean, `npm test` 49/49
  - New endpoints: `/v1/quarantine`, `/v1/baselines/:entityId`, `/v1/policy/:orgId`
- [x] OODA Phase 1-5 strategic analysis — verdict: BUILD, PG would fund this
- [x] PRD v2 — `docs/HOUNDSHIELD-PRD-v2.md` with manager mode + sprint plan
- [x] Brain AI knowledge graph — `lib/brain-ai/knowledge-graph.ts` (BM25, seed knowledge, TTL-aware)
- [x] Brain AI query interface — `lib/brain-ai/brain-query.ts` (ask, addKnowledge, marketCheck)
- [x] CLAUDE.md — manager mode added
- [x] `.claude/settings.json` — model, autoMemoryEnabled, code-review-graph hooks
- [x] `.claude/agents/` — all 8 agents with memory: project and maxTurns: 20
- [x] `.claude/skills/` — all 8 skills including browser-harness, firecrawl-ingest, swarm-orchestrate, anywhere-guard
- [x] `.claude/hooks/pre-commit.sh` — tsc + eslint + npm test gates
- [x] Sprint 1 complete — PR #62 merged 2026-05-03: install.sh, PDF generation + tests, E2E proxy→CUI→PDF, Jordan landing page, /docs/quickstart, Anthropic coral palette, 105/105 tests passing
