# Archived: foreign Jay-Abraham / Struere site

These files were dumped at the HoundShield repo root and are **not part of HoundShield** —
they're a separate Jay Abraham / Struere business-consulting website (WordPress + static
export): the `*.php.html` pages, `index.html`, `_bootstrap.html`, `site/`, `wp-content/`,
`HoundShield_files/`, and vendored `react*.development.js` / `babel.min.js` / `css2`.

Archived here on 2026-06-06 to clean the repo root. Nothing in the HoundShield app
(`compliance-firewall-agent/`) imports any of it — verified the build is green after the move.
**Safe to delete entirely** once you're sure you don't want the markup for reference.
