# HoundShield Audit & Fix Report — 2026-06-20

Branch: `HoundShield/festive-ishizaka-768ef5` · App: `compliance-firewall-agent` (Next.js 16, React 19)
Build gate: **`npm run build` → PASS (exit 0, full route table)** after these changes.

---

## 1. Fixed this session (verified)

| # | Issue (from your brief / screenshots) | Fix | Proof |
|---|----------------------------------------|-----|-------|
| 1 | **Brand name too small** next to the logo (nav showed `HoundShield` at 16px beside a 40px mark) | `NavV3.tsx`: wordmark `text-base` → `text-xl sm:text-2xl`; logo 40 → 44px | preview screenshot — wordmark now large & proportionate |
| 2 | **Footer brand too small** (`text-sm` beside a 48px mark, both footer variants) | `FooterV3.tsx`: both wordmarks `text-sm`→`text-lg`, `font-semibold`→`font-bold` | build pass |
| 3 | **Text too light to read** (low-contrast greys, esp. footer/secondary copy) | `globals.css` tokens darkened: `--hs-ink #0F1E2E→#0A1420`, `--hs-ink-secondary #3D5166→#243646`, `--hs-ink-tertiary #6B8299→#46596C`. Global — every page that uses the tokens gets darker text at once. | preview — hero/body read near-black |
| 4 | **Broken-link sweep** | Static audit of every internal `href` vs. real routes: **0 broken**. The two flagged candidates (`/api/stripe/portal`, the DFARS blog slug) both resolve to real handlers/`[slug]` data. | grep audit |

> Logo consistency: `NavV3` (hover rotate/scale animation) + `FooterV3` are shared across all
> public pages, so the brand renders consistently site-wide. The fixes above propagate everywhere.

---

## 2. BLOCKERS — need you (cannot be coded around)

### 2a. The keys were **not** in your message
You wrote "here are the new updated keys" but no keys were pasted. I will **never** hardcode
keys (they go in Vercel/`.env`, never git). So Supabase/OpenRouter/Brain AI stay in their current
prod state (`payments: missing_key`, `database: demo_mode`) until **you** set them. The exact
commands are in §3. **Re-send the keys via your password manager / Vercel UI, not chat.**

### 2b. Screenshots of issues are pending
You said "I will add the snapshots." The screenshot-driven defect hunt is partly future work —
send them and I'll fix each against the real render.

### 2c. Prod redeploy is gated (intentionally)
`CLAUDE.md`: never `vercel --prod` without approval. And merging this PR to prod **before** keys
are set would ship a still-broken-payments build. Sequence: **set keys → merge → redeploy.**
The PR's Vercel **preview** build verifies the code now; prod waits on you.

---

## 3. Env-var "codes" (run after you have the real keys)

Keys never touch chat or git — `vercel env add` prompts for the value interactively.

```bash
cd compliance-firewall-agent
# OpenRouter — powers ALL Brain AI, chat, agents (consumed in 18+ routes)
vercel env add OPENROUTER_API_KEY production
vercel env add OPENROUTER_API_KEY preview
# Optional: pin Brain AI model + mode
vercel env add BRAIN_AI_MODEL production     # e.g. anthropic/claude-3.5-sonnet
vercel env add BRAIN_AI_MODE  production     # e.g. full

# Supabase — auth + persistence (kills demo_mode)
vercel env add NEXT_PUBLIC_SUPABASE_URL       production
vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY  production
vercel env add SUPABASE_SERVICE_ROLE_KEY      production

# then redeploy
vercel --prod        # ← only with your explicit go-ahead
npx supabase db push # push pending migrations 003 + 004
```

Local dev `.env.local` (names only — fill values yourself, file is gitignored):
```
OPENROUTER_API_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
BRAIN_AI_MODEL=
BRAIN_AI_MODE=full
```

### NVIDIA note (you wrote "nevida")
The codebase reads **only `OPENROUTER_API_KEY`** — there is **no NVIDIA/NIM variable wired**.
NVIDIA NIM is OpenAI-compatible (`https://integrate.api.nvidia.com/v1`). To use it as the Brain AI
backend or a fallback I'd add `NVIDIA_API_KEY` + a base-URL switch in `lib/brain-ai/index.ts`
(~1 small change). **Say the word and I'll wire it.** Until then, "update the NVIDIA key" has
nowhere to land.

---

## 4. Competitor: HoundShield vs. Vanta (and the cloud-DLP pack)

> Verify Vanta's current pricing/features live before quoting them publicly — this is positioning, not a price sheet.

| | **HoundShield** | **Vanta** |
|---|---|---|
| Category | **Runtime AI prompt firewall** — stops CUI/PII/PHI leaving your network *as it happens* | **Compliance automation / trust management** — evidence collection, continuous control monitoring, audit prep |
| Core job | Intercept every LLM prompt, block sensitive data **before** it reaches ChatGPT/Copilot/Claude | Automate SOC 2 / ISO 27001 / HIPAA posture + questionnaires |
| Data boundary | **Local-only / self-host / air-gapped** — prompt content never leaves the customer network | Cloud SaaS |
| CMMC L2 / CUI | Primary wedge — built for DoD contractors (Jordan) | Not the focus |
| Overlap | Both touch SOC2/HIPAA/ISO language | Both touch SOC2/HIPAA/ISO language |
| **Relationship** | **Complementary, not competitive.** Vanta proves you *have* controls; HoundShield *is* the control that prevents AI data spill. | — |

**The asymmetric pitch (keep front-and-center):** every cloud AI-DLP tool (Nightfall, Strac,
Purview) sends your CUI to *their* servers to scan it — that scan is itself a DFARS 7012 spill.
HoundShield scans locally. **Nothing leaves the network.** That is the moat Vanta and cloud DLP
structurally cannot copy.

---

## 5. SEO / AEO / GEO status (already strong — be honest about the rest)

**Technical foundation present & building green:**
- `app/robots.ts`, `app/sitemap.ts`
- JSON-LD structured data: products, `/answers`, blog (`components/seo/JsonLd.tsx`, `lib/seo/structured-data.ts`)
- `metadataBase` + OpenGraph + canonical across layouts
- **AEO** answer pages (`/answers/[slug]`) — the format ChatGPT/Perplexity cite
- **`public/llms.txt`** — AI-crawler roadmap already shipped

**Honest reality on "every AI suggests my site / #1 everywhere":** that is **off-page +
time**, not a commit. The code is largely ready. The growth program is: (1) backlinks
(wire `qwoted-seo-backlinks-skill`), (2) publish answer pages for every CMMC/DFARS buyer
question, (3) earn mentions on sites the models train/retrieve from (G2, Reddit, niche CMMC
blogs), (4) keep `llms.txt` + schema current. I can execute (1)–(4) as a dedicated SEO sprint
— flag it and I'll start with a keyword + answer-gap map.

---

## 6. NOT done this turn (deliberately — needs your decision)
- **Repo installs** — triaged in `future/REPOS-TRIAGE.md`. I did not bulk-clone 8+ repos blind. Tell me which `WIRE` items to integrate + what `ECC` is.
- **Brain AI / agent rewire** — needs the OpenRouter key live first (§2a), then a real test. The keyless FAQ layer already answers demo-critical questions.
- **Post-login content** — "add all info after login" — needs you to specify what (it's the `/command-center` console; tell me which panels/data).

---

## 7. Recommended next steps (in order)
1. **You:** set the env vars (§3) in Vercel — unblocks payments + Brain AI + Supabase.
2. **You:** send the issue screenshots → I fix each against the real render.
3. **Me (on your go):** wire NVIDIA fallback + the chosen `WIRE` repos, one at a time, build green each.
4. **Me:** SEO/backlink sprint (keyword + answer-gap map → publish → backlinks).
5. **Together:** set keys → merge this PR → `vercel --prod` → run the demo-test runbook.
